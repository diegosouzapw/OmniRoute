{
  "name": "@argismonitor/bff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --target=bun --outdir=dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@argismonitor/api-contracts": "workspace:*",
    "hono": "^4.0.0",
    "@hono/zod-validator": "^0.4.0",
    "@hono/node-server": "^1.0.0",
    "zod": "^4.0.0",
    "@hono/trpc-server": "^0.1.0",
    "@trpc/server": "^11.0.0",
    "msgpackr": "^1.11.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.0.0"
  }
}
