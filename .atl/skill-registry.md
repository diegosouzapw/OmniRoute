# OmniRoute — Skill Registry

> Auto-generated by sdd-init. Last updated: 2026-04-21.

## Project Context
- **Project**: omniroute
- **Stack**: Next.js 16, TypeScript 5.9, better-sqlite3, Tailwind CSS v4, Electron
- **Path**: E:\GitHub\OmniRoute
- **Persistence**: engram

## Testing Capabilities
| Type | Tool | Command |
|------|------|---------|
| Unit | Node.js `--test` runner | `node --import tsx/esm --test tests/unit/*.test.ts` |
| Integration | Node.js `--test` runner | `npm run test:integration` |
| MCP/AutoCombo | Vitest | `npm run test:vitest` |
| E2E | Playwright | `npm run test:e2e` |
| Protocol | MCP+A2A clients | `npm run test:protocols:e2e` |
| Coverage | c8 | `npm run test:coverage` (60% gate) |

**Strict TDD**: ✅ Enabled (Node.js test runner present)

## Skill Registry

### SDD Workflow (Spec-Driven Development)
| Skill | Trigger | Purpose |
|-------|---------|---------|
| `sdd-init` | `sdd init`, `iniciar sdd` | Initialize SDD context |
| `sdd-propose` | Orchestrator: create proposal | Change proposal with intent, scope, approach |
| `sdd-spec` | Orchestrator: write specs | Delta specs with requirements and scenarios |
| `sdd-design` | Orchestrator: write design | Technical design document |
| `sdd-tasks` | Orchestrator: break down | Implementation task checklist |
| `sdd-apply` | Orchestrator: implement | Write code following specs and design |
| `sdd-verify` | Orchestrator: validate | Validate implementation matches specs |
| `sdd-archive` | Orchestrator: archive | Sync delta specs to main, archive change |
| `sdd-onboard` | Orchestrator: onboarding | Guided SDD workflow walkthrough |
| `sdd-explore` | Orchestrator: investigate | Explore ideas before committing |

### Quality Assurance
| Skill | Trigger | Purpose |
|-------|---------|---------|
| `judgment-day` | `judgment day`, `juzgar`, `dual review` | Parallel adversarial review (2 judges) |
| `go-testing` | Go tests, Bubbletea TUI | **NOT RELEVANT** — OmniRoute is TypeScript |

### Collaboration
| Skill | Trigger | Purpose |
|-------|---------|---------|
| `branch-pr` | Creating PR, preparing for review | PR workflow (issue-first enforcement) |
| `issue-creation` | Creating issue, reporting bug | Issue creation workflow |
| `skill-creator` | Creating new AI skills | Create agent skills |

### Utilities
| Skill | Trigger | Purpose |
|-------|---------|---------|
| `skill-registry` | `update skills`, `skill registry` | Scan and update this registry |

## Skill Locations
- **User skills**: `~/.claude/skills/` (canonical)
- **User skills**: `~/.config/opencode/skills/` (compatibility, synced)
- **Project skills**: None (OmniRoute does not have `.claude/`, `.agent/`, or `skills/` directories)

## Relevant Skills for OmniRoute
Most relevant for this project:
1. **sdd-*** series — full feature development workflow
2. **judgment-day** — adversarial review before releases
3. **branch-pr** — PR workflow with issue-first enforcement
4. **skill-registry** — update this file after skill changes

## Engram Persistence
Three observations saved to engram:
1. `sdd-init/omniroute` (architecture) — full project context
2. `sdd/omniroute/testing-capabilities` (config) — testing setup
3. `skill-registry` (config) — this registry

## Next Recommended Steps
1. Run `npm run typecheck:core` to verify TypeScript setup
2. Run `npm run test:unit` to verify test runner works
3. Explore `tests/unit/` to understand testing patterns
4. Read `AGENTS.md` for architecture deep-dive

## Risks
1. **Complex monorepo** — open-sse is a workspace package, paths differ from src/
2. **Multiple test runners** — Node.js `--test` for unit, Vitest for MCP, Playwright for E2E
3. **SQLite state** — tests use real DB, may need `DISABLE_SQLITE_AUTO_BACKUP=true`
4. **ESM-only** — no CommonJS, all imports must use ES module syntax
5. **Strict coverage gate** — 60% minimum required
