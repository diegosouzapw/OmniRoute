- **feat(reasoning):** adaptive reasoning effort (`auto`) — the gateway resolves the thinking budget per user turn from deterministic request-shape signals (stateless per-turn pin) instead of forwarding a literal `auto`, covering every harness (Claude Code, Cursor, Codex, opencode, Hermes) in one place, pre-translation. Opt in via `X-OmniRoute-Effort: auto` or a model's `defaultReasoningEffort: "auto"`; any explicit client reasoning field always wins ([#13448](https://github.com/diegosouzapw/OmniRoute/pull/13448))
