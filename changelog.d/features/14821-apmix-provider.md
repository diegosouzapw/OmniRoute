- **feat(providers):** add Apmix (apmix.ai) as an OpenAI-compatible, subscription-based provider — one monthly plan with a weighted-token allowance, chat-completions + Responses API at `https://api.apmix.ai/v1`, live `/v1/models` discovery keyed to the plan's model union (seed catalog as offline fallback), and a usage fetcher (`GET /v1/usage`) surfacing the monthly allowance, self-imposed daily/weekly caps and top-up balance in Dashboard › Limits with quota-aware preflight ([#14821](https://github.com/diegosouzapw/OmniRoute/pull/14821)).
