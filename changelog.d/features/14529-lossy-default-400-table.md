- **fix(compression):** keep lossy compression off the default path. Dedup and whitespace folding still run; summaries, relevance filters, and style rewrites need `x-omniroute-compression: allow-lossy`, `engine:<id>`, or a named combo. Combo 400 handling is a decision table: model-scoped wrappers advance, and a bare `invalid` / `Bad Request` no longer stops the combo ([#14529](https://github.com/diegosouzapw/OmniRoute/pull/14529)) — thanks @RaviTharuma
