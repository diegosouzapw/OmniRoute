- **feat(api):** authenticate Claude Code (and any client) to `/v1/*` with a Microsoft Entra ID token instead of a static API key — Entra security groups map onto existing OmniRoute key groups, so per-key rate limits, model allowlists, budgets and combo permissions apply per person and usage is attributed per person. Ships a dependency-free sign-in helper (`scripts/cli/omniroute-sso.mjs`) for Claude Code's `apiKeyHelper`. Opt-in and additive: static API keys keep working ([#14665](https://github.com/diegosouzapw/OmniRoute/pull/14665)).
