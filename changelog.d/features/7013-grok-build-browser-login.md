- **feat(oauth):** Add a one-click browser (PKCE) login for Grok Build (`grok-cli`), reusing the same `auth.x.ai` authorize/token endpoints and public client id as the sibling `xai-oauth` provider on its own loopback port, while keeping the existing paste-token/`auth.json` import flow available as a fallback for headless/remote installs ([#7013](https://github.com/diegosouzapw/OmniRoute/issues/7013))
