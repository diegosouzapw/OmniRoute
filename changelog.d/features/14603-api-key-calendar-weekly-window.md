- **feat(usage):** Per-key weekly USD limits can use a calendar week instead of the earliest upstream account reset. `OMNIROUTE_API_KEY_WEEKLY_WINDOW=calendar` gives every key the local Monday 00:00 week in `OMNIROUTE_API_KEY_WEEKLY_WINDOW_TIMEZONE` (default: the process timezone); unset keeps the current behavior ([#14603](https://github.com/diegosouzapw/OmniRoute/pull/14603), closes [#14601](https://github.com/diegosouzapw/OmniRoute/issues/14601)) — thanks @xiaoyaner0201
