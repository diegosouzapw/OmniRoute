- **feat(db):** opt-in PostgreSQL backend (`OMNIROUTE_DATABASE_URL`) so several replicas can share one database, with a one-shot SQLite import (`OMNIROUTE_DATABASE_IMPORT_SQLITE=1` / `npm run db:migrate-to-postgres`), a `postgres` compose profile, and a warm-cache cold path for `getSyncedCapability()` ([#13118](https://github.com/diegosouzapw/OmniRoute/pull/13118)) — thanks @dpozimski
