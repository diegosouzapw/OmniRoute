- **ci:** drain the ESLint-errors hard gate on `release/v3.8.51` (base-red #13866) — five `no-unused-vars` errors introduced by #12553 and #13446 in `src/shared/services/cliRuntime.ts` and three unit test files had no frozen suppression entries, so every release-green run reported 5 hard errors; remove the genuinely dead imports/locals and `_`-prefix the one unused handler parameter (no behavior change, suites re-run green).
