- **fix(responses):** Preserve tools declared through `additional_tools` input items when Responses API requests are translated to Chat Completions. Top-level and input-item tool declarations now share the same provider-neutral validation and conversion path, allowing agent clients to use custom, function, namespace, and supported client-executed tools through any downgraded upstream.
