- **fix(grok-cli):** Grok Build (grok-cli) no longer 422s on Codex requests that declare freeform `custom` tools (`apply_patch`, or lite mode's namespaced `exec`) or carry their tools in `additional_tools` input items: tools are merged and sent as function tools on the same-format lane, and returned calls come back as `custom_tool_call` items with streamed input deltas ([#14738](https://github.com/diegosouzapw/OmniRoute/pull/14738))
