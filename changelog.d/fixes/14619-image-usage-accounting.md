- **fix(usage):** `/v1/images/generations` and `/v1/images/edits` (direct, provider-scoped and combo) now record successful calls in `usage_history` against the API key and the connection that served them when the upstream reports token usage, so per-key USD limits and per-key usage reports include image calls; image `call_logs` rows on the combo and edit paths now carry the API key and connection ([#14619](https://github.com/diegosouzapw/OmniRoute/pull/14619), refs [#14618](https://github.com/diegosouzapw/OmniRoute/issues/14618))
