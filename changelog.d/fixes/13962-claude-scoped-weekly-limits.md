- **fix(usage):** Claude OAuth usage reads every per-model weekly cap Anthropic now reports in `limits[]` (`weekly_scoped`, e.g. Fable) into the display-only `modelQuotas` map as `weekly <model> (7d)`, replacing the Fable-only parser, so a scoped cap that is the active limit on the seat shows up on the dashboard and in the Provider Limits cache without entering account-wide routing quotas ([#13962](https://github.com/diegosouzapw/OmniRoute/pull/13962)) — thanks @dpozimski
