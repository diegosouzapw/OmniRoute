- **fix(combo):** the same-target retry after a pre-content streaming upstream error works again ([#14547](https://github.com/diegosouzapw/OmniRoute/issues/14547)) — #14314 started carrying the upstream detail in the quality reason (`streaming upstream error: <detail>`), which silently disabled #13630's exact-match retry gate so protected/pinned targets failed instead of retrying once; the gate now recognizes both the bare and the detailed reason.
