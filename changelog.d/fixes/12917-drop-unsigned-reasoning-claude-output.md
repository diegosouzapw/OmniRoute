- **fix(translator):** drop unsigned reasoning from streaming and non-streaming OpenAI-compatible responses translated for Claude instead of emitting invalid `thinking` blocks or exposing private chain-of-thought as visible text, preventing Anthropic signature-replay failures after cross-provider routing ([#12917](https://github.com/diegosouzapw/OmniRoute/pull/12917)) — thanks @atescivitci-cmd
