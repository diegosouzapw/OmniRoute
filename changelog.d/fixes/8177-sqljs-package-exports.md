- **fix(build):** resolve the sql.js WASM asset through its public package entrypoint so Turbopack no longer reports the private `sql.js/package.json` subpath as missing ([#8177](https://github.com/diegosouzapw/OmniRoute/pull/8177)) — thanks @backryun
- **fix(quality):** reconcile release-branch gate drift across lint and complexity baselines, error sanitization, compression metadata/docs, environment contracts, mutation coverage, dashboard typing, quota-reset expectations, catalog/sidebar fixtures, and cross-platform tests ([#8177](https://github.com/diegosouzapw/OmniRoute/pull/8177)) — thanks @backryun
