- fix(electron): strip hashed native-module duplicates from the Electron standalone and fix the removal path. Enables the already-proven `patchTurbopackChunks` on the Electron packaging step (so server chunks stop `require()`-ing hashed names like `better-sqlite3-<hash>`), and points `removeNativeModules()` at the real `NEXT_DIST_DIR` (`.build/next`) instead of a hardcoded `.next` that never matched — so the stale Node-ABI native copies that shadowed the Electron-ABI-rebuilt `better-sqlite3` are actually deleted. Also strips `sqlite-vec`. Fixes the Electron `Internal Server Error` (better-sqlite3 ABI mismatch → sql.js WASM OOM fallback). `removeNativeModules()` extracted to a pure module with unit tests (#7123, closes #7082)
