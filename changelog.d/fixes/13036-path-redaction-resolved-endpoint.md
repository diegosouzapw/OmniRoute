- **fix(security):** the public error sanitizer no longer collapses everything after a source path into `<path>` — a resolved filename extension now ends the redaction span, so operators get the rate-limit / quota / unsupported-parameter reason back and credential labels still reach the credential redactor as `Authorization: [REDACTED]`; extensionless paths keep failing closed. Turns `stream-handler-public-error-boundary` green on `release/v3.8.51` ([#13036](https://github.com/diegosouzapw/OmniRoute/pull/13036)) — thanks @ggiak
