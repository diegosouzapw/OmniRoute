- **fix(sse):** the non-streaming leg stopped refreshing credentials after a `401`, dropped the body-derived rate-limit lock, and discarded the upstream error `code`/`type` — all lost when the provider execution pipeline was extracted. A Copilot token was never retried, `Please retry after 20s` never drained the runtime reservoir, and a config-class `422` (Antigravity missing project) degraded into a generic account cooldown ([#12732](https://github.com/diegosouzapw/OmniRoute/issues/12732))
- **fix(db):** a legacy `call_logs` table no longer aborts startup — the provider-stats index is created after the column healing runs, not before it, so an install predating `request_type` opens instead of failing with `no such column` ([#12732](https://github.com/diegosouzapw/OmniRoute/issues/12732))
- **fix(catalog):** a malformed row in the operator-writable custom-models blob no longer kills every `auto/*` pool, and operator custom models no longer leak into the live-sync reader and shadow a registry model with a parentless duplicate ([#12732](https://github.com/diegosouzapw/OmniRoute/issues/12732))
- **fix(sse):** codex image generation no longer crashes reading fields off an upstream error body that error-boundary hardening had already sanitized ([#12732](https://github.com/diegosouzapw/OmniRoute/issues/12732))
- **fix(tests):** realign four guards that the same day's merges left asserting the old behavior — the provider-count and reserved-prefix totals after GreenPT and EURouter, the GLM stream buffer slot that `#12925` turned from a dropped extra argument into a declared parameter, and the injection scan bound that `#13104` reshaped from a head-only window into head-plus-tail. Every guard keeps the invariant it was written to protect ([#12732](https://github.com/diegosouzapw/OmniRoute/issues/12732))
