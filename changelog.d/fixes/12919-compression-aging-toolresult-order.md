- **fix(compression):** When aging or aggressively rewriting a user message whose only content is a `tool_result` block, append the compression annotation after the block instead of prepending a `text` block ahead of it, so Anthropic no longer rejects the request with 400 "tool_use ids were found without tool_result blocks immediately after" ([#12919](https://github.com/diegosouzapw/OmniRoute/pull/12919)).
