- **fix(executors):** auto-learn now recognizes Anthropic's "`X` is deprecated for this model" wording (previously only "Unsupported parameter: X"), so a retired sampling param on a model not covered by the static strip rules — e.g. `temperature` on `claude-opus-5-5` — is stripped and retried instead of surfacing a 400 to the client ([#14561](https://github.com/diegosouzapw/OmniRoute/pull/14561))
