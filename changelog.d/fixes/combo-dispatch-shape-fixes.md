- **fix(combo):** tiny-budget reasoning probes (max_tokens < 256) whose upstream 200 burned the whole budget on thinking are answered with a truncated 200 (`finish_reason: "length"`) in both combo dispatchers instead of a 502 + model lockout (#10281 combo-path parity)
- **fix(translator):** opencode console gateways (opencode-go / opencode-zen) now get the reasoning replay sentinel on cache misses for thinking-mode tool turns, and the Responses converter emits it as a `reasoning_text` item — upstream 400 "The reasoning_text in the thinking mode must be passed back to the API" no longer takes out deepseek/glm multi-turn traffic; DeepSeek-direct absent-field behavior unchanged (#9573/#9610 contract preserved)
- **fix(translator):** GLM-family upstreams get a synthetic `(continue)` user turn on the same-format (openai→openai) lane when history has no user turn, mirroring the Claude-lane `_ensureUserTurn` behavior — upstream 400 `[1214] The messages parameter is illegal` fixed
- **fix(embeddings):** per-item string inputs are clamped to 20k chars before upstream dispatch so oversized documents no longer fail with "Invalid 'input': maximum context length is 8192 tokens"
- **fix(config):** egress-IP lockout provider family is overridable via `OMNIROUTE_EGRESS_IP_LOCK_PROVIDERS` (`none` disables the shared-IP cascade parking for paid per-workspace plans)
