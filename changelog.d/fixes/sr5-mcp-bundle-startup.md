- **fix(mcp):** Restore MCP server startup from the published bundle — the quota healthy override from #14555 made `quotaPreflight` import `quotaCache` back through `usage.ts`, closing an ESM init cycle that deadlocked the esbuild bundle ("unsettled top-level await"); the override state now lives in a dependency-free `quotaCacheState` leaf, with the #14555 park/override behavior unchanged. Refs #14547.
