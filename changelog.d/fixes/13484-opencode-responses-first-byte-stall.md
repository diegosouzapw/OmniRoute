- **fix(opencode):** a streamed Responses reply that sends headers and then nothing no longer holds the request for 80 seconds before failing: the account is cooled down and the request moves to the next one after 15 seconds, and a second stall fails fast (`RESPONSES_FIRST_BYTE_TIMEOUT_MS`, `0` disables) ([#13484](https://github.com/diegosouzapw/OmniRoute/pull/13484)) — thanks @maxmad64bis
