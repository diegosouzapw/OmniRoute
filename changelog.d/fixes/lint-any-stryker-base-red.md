- **fix(quality):** Cleared two base-red gates on the release tip — replaced 320 `@typescript-eslint/no-explicit-any` violations across 5 recently-landed test files with real, derived types (no suppressions added) and registered 4 covering unit test files that were missing from `stryker.conf.json` `tap.testFiles`, restoring `npm run lint` and `npm run check:mutation-test-coverage` to green.
