- **fix(reasoning-routing):** a `force` + `none` rule now emits an explicit `reasoning_effort: "none"` instead of only clearing reasoning params, so providers whose thinking defaults ON (e.g. DeepSeek V4 behind the native Responses API) actually stop thinking instead of failing later turns with `The reasoning_text in the thinking mode must be passed back to the API`; Command Code clamps the literal `none` to `low`, mirroring its existing `minimal` clamp ([#14873](https://github.com/diegosouzapw/OmniRoute/pull/14873)) — thanks @TheRealAlexV
