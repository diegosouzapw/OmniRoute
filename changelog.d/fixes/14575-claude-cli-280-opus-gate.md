- **fix(providers):** bump the pinned Claude Code client version 2.1.258 → 2.1.280 so `claude-opus-5-5` stops being rejected with `400: Claude Code 2.1.258 does not support this model; version 2.1.280 or newer is required`. Anthropic gates the new Opus 5.5 on the advertised `claude-cli` version in the `User-Agent` / billing headers; the captured 2.1.258 pin is below that floor, so every Claude-family provider (claude, cc, bedrock, vertex) failed model access even though the model id itself is registered. The env override `CLAUDE_CODE_CLIENT_VERSION` (#12417) only covers the getter paths — the pin also feeds module-load snapshots (`CLAUDE_CLI_USER_AGENT`, `CLAUDE_CODE_COMPATIBLE_USER_AGENT`, `DEFAULT_CLAUDE_CODE_VERSION`), so the constant itself must move until a newer signed binary is captured. Build revision (`1e2`), SDK package (0.112.1) and runtime (v26.3.0) stay unchanged; only the CLI patch version moves.
