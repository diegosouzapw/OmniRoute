- **fix(compression):** the Settings "Effective pipeline" preview now derives the plain default exactly like a live request (`deriveDefaultPlanFromConfig`) — on installs whose engines map was never saved from the panel it follows the legacy `defaultMode` the runtime dispatches on, instead of the display-only engines backfill, so the preview can no longer disagree with the pipeline that actually runs (regression of #12063) ([#14700](https://github.com/diegosouzapw/OmniRoute/pull/14700)) — thanks @gonisulaimann
