- **fix(compression):** the Settings "Effective pipeline" preview now shows the plan a header-less request actually runs — with a lossy profile (rtk, caveman, …) active it previews the downgraded safe `session-dedup` + `lite` pipeline instead of claiming the lossy steps run on every request (regression of #12063) ([#14700](https://github.com/diegosouzapw/OmniRoute/pull/14700)) — thanks @gonisulaimann
