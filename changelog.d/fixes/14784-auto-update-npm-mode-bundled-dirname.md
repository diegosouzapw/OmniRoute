- **fix(api):** Global npm installs are detected as npm again, so the dashboard's "Manual Update" works instead of showing "Not a git repository. Download source or use npm install -g.": the install-mode check read `__dirname`, which the Next.js server bundle freezes to `"/ROOT/src/lib/system"`, and now falls back to the entry script (`argv[1]`, or `pm_exec_path` under pm2) when it lives inside an installed `omniroute` package ([#14784](https://github.com/diegosouzapw/OmniRoute/pull/14784), closes [#14782](https://github.com/diegosouzapw/OmniRoute/issues/14782)) — thanks @cfigueiroa
