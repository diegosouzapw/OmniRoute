# DAG / WBS

1. Confirm current repo state and detect primary language.
2. Update `Taskfile.yml` to keep the common tasks while tightening `clean`.
3. Validate the Taskfile with `task --list` and targeted task execution.
4. Publish the branch, open a PR, and merge it with the requested GitHub flow.
