# v13 Plan — 71-Pillar Cycle 2 P0 Remediation (Sprint 2)

**Date:** 2026-06-20 22:30 PDT | **Branch:** `chore/v13-71-pillar-cycle-2-p0-2026-06-20` (to create) | **Owner:** worklog-schema circle + fleet L6 audit

## Scope (from `findings/2026-06-20-71-pillar-cycle-2-probe.md`)

v13 closes the **next 5-8 P0 pillars** identified in cycle 2. v12 closed 11/47 (23%); v13 targets 5-8 of remaining 26 + 2 v12-introduced.

## 8 Tracks (T1-T8)

| # | Pillar | Title | Target repo | Effort | Impact |
|---|---|---|---|---|---|
| **T1** | L21 | cargo-fuzz harness | `pheno-port-adapter` | 3h | Closes 1 P0 (fuzz harness) |
| **T2** | L33 | devshell.nix per substrate | 4 nested repos | 1h | Closes 1 P0 (reproducible dev env) |
| **T3** | L48 | cargo-cyclonedx SBOM | 3 nested repos | 2h | Closes 1 P0 (SBOM diff per CI) |
| **T4** | L49 | SLSA L3 provenance | parent monorepo | 4h | Closes 1 P0 (supply-chain attestation) |
| **T5** | L50 | cosign signature | parent monorepo | 3h | Closes 1 P0 (signed releases) |
| **T6** | L11.1 | chaos test in CI | `pheno-port-adapter` | 1h | Closes 1 v12-introduced P0 |
| **T7** | L29.1 | justfile-verify in pre-commit | 14 nested repos | 1h | Closes 1 v12-introduced P0 |
| **T8** | L66 | llms.txt per repo (re-publish) | 14 nested repos | 1h | Closes 1 P0 (LLM-tractable metadata) |

**Total effort:** ~16h sequential / ~5h parallel (3-4 tracks at a time)
**P0 closure target:** 8 pillars (lift 26 → 18, mean 2.6 → 2.7)
**Fleet mean score target:** 2.4 → 2.55 (cycle 2 → cycle 3)

## Track details

### T1 — L21 cargo-fuzz harness (pheno-port-adapter)

```toml
# pheno-port-adapter/Cargo.toml addition
[package.metadata.fuzz]
fuzz_dir = "fuzz"

[[bin]]
name = "fuzz_tcp_endpoint_parse"
path = "fuzz/fuzz_tcp_endpoint_parse.rs"
```

```rust
// fuzz/fuzz_tcp_endpoint_parse.rs
#![no_main]
use libfuzzer_sys::fuzz_target;
fuzz_target!(|data: &[u8]| {
    let _ = pheno_port_adapter::adapters::tcp::TcpAdapter::parse_endpoint(
        std::str::from_utf8(data).unwrap_or("")
    );
});
```

CI target: `just fuzz` (5 min nightly). Closes L21 1→3.

### T2 — L33 devshell.nix per substrate (4 nested repos)

`pheno-flags/devshell.nix`, `pheno-errors/devshell.nix`, `pheno-port-adapter/devshell.nix`, `pheno-otel/devshell.nix`. Each pins rustc 1.83, cargo-xtask, criterion, cargo-fuzz, dprint. Closes L33 1→3.

### T3 — L48 cargo-cyclonedx SBOM (3 nested repos)

`cargo install cargo-cyclonedx --locked && cargo cyclonedx --format json --override-filename sbom.json`. Diff two SBOMs in CI; fail on new GPL/AGPL addition. Closes L48 1→3.

### T4 — L49 SLSA L3 provenance (parent monorepo)

`slsa-github-generator` GitHub Action. Provenance attestation in `.slsa/`. L3 level = hardened runner + non-forgeable source. Closes L49 1→3.

### T5 — L50 cosign signature (parent monorepo)

`cosign sign-blob --key cosign.pub <release.tar.gz>`. Public key in `cosign.pub`; verify in release CI. Closes L50 1→3.

### T6 — L11.1 chaos test in CI

```yaml
# .github/workflows/chaos.yml
name: chaos
on: [push, pull_request]
jobs:
  chaos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --release --tests -- --test-threads=1 chaos_
```

Closes L11.1 0→3 (cycle 2 v12-introduced P0).

### T7 — L29.1 justfile-verify in pre-commit

```yaml
# .pre-commit-config.yaml addition
- repo: local
  hooks:
    - id: justfile-verify
      name: justfile-verify
      entry: just justfile-verify
      language: system
      pass_filenames: false
```

Closes L29.1 0→3 (cycle 2 v12-introduced P0).

### T8 — L66 llms.txt per repo (re-publish)

Each substrate gets `llms.txt` at root (ADR-022 §2.3 schema: name, purpose, when, when-not, 5-line quickstart, env vars, key types, 1 example). Closes L66 2→3.

## Dependencies

```
T1 (fuzz) -|
T2 (nix) -|- independent, can parallelize
T3 (SBOM) -|
T4 (SLSA) - needs T3 done (SLSA wraps SBOM)
T5 (cosign) - needs T4 done (cosign signs SLSA)
T6 (chaos CI) - needs no other
T7 (just pre-commit) - needs no other
T8 (llms.txt) - needs no other
```

**Parallel waves:**
- Wave A (parallel): T1, T2, T3, T6, T7, T8 (6 tracks, all independent)
- Wave B (sequential after A): T4 → T5

## Acceptance criteria

- [ ] All 8 commits land on `chore/v13-71-pillar-cycle-2-p0-2026-06-20`
- [ ] All 8 PRs OPEN on `KooshaPari/argis-extensions`
- [ ] Cycle 3 71-pillar probe shows 18 P0 remaining (down from 26)
- [ ] Fleet mean score 2.55+ (up from 2.4)
- [ ] 0 P0 reintroduced

## Out of scope for v13

- L22 (bench dashboard) - defer to v14
- L23-L27 (mutation / type hints / proptest) - fleet L6 audit owns
- L52-L55 (mTLS/OIDC/JWKS/vault) - ADR-046 owns; v15+
- L58-L63 (tracing / SLO / chaos game-days) - fleet L6 owns

## Open questions

1. **T4 SLSA L3** requires `slsa-github-generator` in private infra — does Phenotype have access? Check with fleet ops.
2. **T5 cosign** key storage — KMS or local? Default to GitHub OIDC for ephemeral keys.
3. **T8 llms.txt** for archived repos — `pheno-tracing`, `phenotype-ops` can't push; generate as PR comment instead.

Refs: cycle 2 (`findings/2026-06-20-71-pillar-cycle-2-probe.md`), v12 closure (`findings/2026-06-20-v12-closure.md`), ADR-024, ADR-041 (refresh cadence)
