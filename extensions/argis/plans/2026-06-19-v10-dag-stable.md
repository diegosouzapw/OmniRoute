# DAG v10 — Phenotype Fleet W5 Wrap-up + 44-PR Triage

**Date authored:** 2026-06-19
**Status:** SPEC READY (launching in this session)
**Supersedes:** v9 (`plans/2026-06-19-v9-dag-stable.md`) — v9 still WIP (parental subagent DAG running on the 84 ready `side-NN` tasks; AGENTS.md says T9-T23 v8 carryover)
**Extends:** v9 side-DAG fillers + v8 carryover (T9-T23) + new v10 L5-118 44-PR triage wave + L5-119 fleet ops gate

---

## 1. Executive Summary

v10 is the **W5 fleet wrap-up**. v9 launched today (2026-06-19 01:23 PDT) with 280 tasks / 210 PRs targeting the W2-W3-W4 governance wave. Between v9 launch and now (~19h), the fleet absorbed substantial parallel work:

- 5/5 Wave-2 shards (S12-S16) delivered in this session — `phenotype-lexer-rs`, `phenotype-sdk`, `phenotype-bot-framework`, `phenotype-discord-adapter`, `PhenoFastMCP-rust` all created/scaffolded.
- 1 v8 PR (HeliosLab #128 SLSA) merged.
- 2 PRs (#109 journeys SLSA, #127 HeliosLab cargo-deny) still open.
- 0 HexaKit #227 PR (was merged by another session).
- **44 NEW open PRs** discovered at 20:21 PDT that weren't in v9's scope:
  - 9 `phenotype-journeys` dependabot (base64, crossterm, x11-clipboard, wl-clipboard, which, image, notify, dirs, chrono) — all MERGEABLE
  - 8 `HexaKit` (7 dependabot + 1 fix/drain-last-phenoshared-pin) — all MERGEABLE
  - 27 `phenotype-tooling` PRs:
    - 8 dependabot (dependabot group bumps, SLSA generator v2.1.0, phenoShared ci.yml) — UNKNOWN mergeable
    - 16 CONFLICTING feature PRs (test/fix/feat/docs/ci/absorb from 2026-06-08) — need rebase or close
    - 1 **integration/consolidate** mega-PR (`chore(consolidate): unify all open PR work onto one branch`) — UNKNOWN but likely unifies the 16 conflicting
    - 3 absorb/docs PRs (#124, #129, #130, #132-#133)

**v10's job**: bring the 44 open PRs to zero, capture the orphan branches, refresh parent repo state (currently ahead 477 / behind 77), then resume v9's side-DAG fillers (84 ready `side-NN` tasks).

---

## 2. Track Inventory

| Track | Title | Priority | Effort | Owner |
|---|---|---|---|---|
| **T24** | PR triage — 17 low-risk MERGEABLE dependabot/fix PRs | P0 | ~20 min | forge-1 (me) |
| **T25** | 16 CONFLICTING tooling PRs triage — rebase or close (depends on T26) | P0 | ~30 min | forge-2 |
| **T26** | `integration/consolidate` mega-PR (PR #122) — verify and merge first | P0 | ~15 min | forge-1 (me) |
| **T27** | Push parent `repos/` to clean (ahead 477 → 0) | P1 | ~10 min | forge-1 (me) |
| **T28** | Worktree debt cleanup — HexaKit stale worktrees + terrain/water orphan branches | P2 | ~10 min | forge-3 |
| **T29** | Resume v9 side-DAG fillers (84 ready `side-NN`) — spawn wave | P1 | ~60 min | forge-4 |
| **T30** | dagctl re-bootstrap (binary was removed) | P2 | ~15 min | forge-1 (me) |

**Total: 7 tracks, ~120 min wall-clock with 4-way parallelism.**

---

## 3. Per-Track Task List

### T24 — PR Triage (17 low-risk MERGEABLE)

- [x] Read all 17 PRs to verify CI green
- [ ] Merge journeys #99-#107 (9 dependabot PRs, MERGEABLE)
- [ ] Merge HexaKit #280-#284 (5 dependabot PRs, MERGEABLE)
- [ ] Merge HexaKit #285 (fix/drain-last-phenoshared-pin)
- [ ] Merge HexaKit #228 (uv group bump, UNKNOWN — rebase if needed)
- [ ] Merge HeliosLab #127 (cargo-deny all-features, leftover from v8)
- [ ] Verify all merged PRs have passing CI post-merge
- [ ] Close any orphaned branches that were merged

### T25 — 16 CONFLICTING tooling PRs

- [ ] Read all 16 conflicting PRs to assess value vs. age
- [ ] Check if `integration/consolidate` (#122) already contains their work
- [ ] If yes: close all 16 as superseded by #122
- [ ] If no: rebase the highest-value 2-3 onto current main and merge
- [ ] Close the rest as stale (orphaned branches)

### T26 — `integration/consolidate` mega-PR

- [ ] Review PR #122 diff stats (probably 200+ files)
- [ ] Check if CI passes
- [ ] Merge if green; close if red
- [ ] If red: triage which sub-PRs are recoverable and rebase those

### T27 — Parent repo push cleanup

- [ ] Review the 477-ahead commit log
- [ ] Identify the v8/v9 bulk-push commits
- [ ] Check no merge conflicts on `main`
- [ ] Push to `phenotype-apps:archive/2026-06-19-v10-wrapup` branch
- [ ] Or: `git reset --soft HEAD~477` and re-commit as a single meta-commit

### T28 — Worktree debt cleanup

- [ ] List all worktrees across 5 active focus repos
- [ ] Remove stale worktrees (branches already merged)
- [ ] Verify orphan branches: `phenotype-terrain`/`phenotype-water` no longer exist locally
- [ ] Document cleanup in `findings/2026-06-19-T28-worktree-cleanup.md`

[STATUS: DONE - 0 prunable worktrees, 1 active main checkout + 3 sub-repo worktrees, see findings/2026-06-19-t28-worktree-debt.md]

### T29 — v9 side-DAG fillers (resume)

- [ ] Inspect 84 ready `side-NN` tasks in `FLEET_DAG.db`
- [ ] Group by repo and kind (audit/sota/guardrail)
- [ ] Spawn 4 parallel forge subagents (forge-2/3/4 + a new one) — each handles ~21 tasks
- [ ] Each subagent claims in `dagctl` (after T30 re-bootstrap) or uses direct shell if dagctl unavailable
- [ ] Aggregate results into `findings/2026-06-19-T29-v9-side-dag-wave.md`

### T30 — dagctl re-bootstrap

- [ ] Locate `dagctl.go` source (likely in `phenodag/` or `KooshaPari/phenodag`)
- [ ] `go build -o dagctl` and place at `./dagctl`
- [ ] Verify shared-lock works: `dagctl pick -agent forge-1 -db FLEET_DAG.db`
- [ ] If source missing: fork `KooshaPari/phenodag#13` (the dagctl absorption PR) and rebuild
- [ ] Document build instructions in `findings/2026-06-19-T30-dagctl-rebootstrap.md`

[STATUS: CANCELLED - dagctl is Go binary already absorbed into phenodag via L5-114, see findings/2026-06-19-t30-dagctl-bootstrap.md]

---

## 4. Topological Sort

```
T26 (verify #122) ──► T25 (triage 16 conflicting) ──► T24 (merge low-risk) ──► T29 (resume v9)
                                                                                ▲
T30 (dagctl re-bootstrap) ──────────────────────────────────────────────────────┘
T27 (parent repo push) [independent]
T28 (worktree cleanup) [independent]
```

**T26 → T25 → T24** is the critical path for PR closure. T29 can start in parallel after T30. T27 and T28 are independent.

---

## 5. PR Matrix

### T24 — 17 low-risk merges (target repos)

| Repo | PR | Head | Title (truncated) |
|---|---|---|---|
| phenotype-journeys | #99 | dependabot/cargo/chrono-0.4.45 | bump chrono |
| phenotype-journeys | #100 | dependabot/cargo/dirs-6.0.0 | bump dirs |
| phenotype-journeys | #101 | dependabot/cargo/notify-8.2.0 | bump notify |
| phenotype-journeys | #102 | dependabot/cargo/image-0.25.10 | bump image |
| phenotype-journeys | #103 | dependabot/cargo/which-8.0.3 | bump which |
| phenotype-journeys | #104 | dependabot/cargo/wl-clipboard-rs-0.9.3 | bump wl-clipboard-rs |
| phenotype-journeys | #105 | dependabot/cargo/x11-clipboard-0.9.3 | bump x11-clipboard |
| phenotype-journeys | #106 | dependabot/cargo/crossterm-0.29.0 | bump crossterm |
| phenotype-journeys | #107 | dependabot/cargo/base64-0.22.1 | bump base64 |
| HexaKit | #280 | dependabot/github_actions/dtolnay/rust-toolchain | update rust-toolchain |
| HexaKit | #281 | dependabot/github_actions/actions/setup-python-6.2.0 | bump setup-python |
| HexaKit | #282 | dependabot/github_actions/peter-evans/create-pull-request-8.1.1 | bump create-pull-request |
| HexaKit | #283 | dependabot/github_actions/astral-sh/setup-uv-8.2.0 | bump setup-uv |
| HexaKit | #284 | dependabot/github_actions/taiki-e/install-action-2.82.0 | bump install-action |
| HexaKit | #285 | fix/drain-last-phenoshared-pin-cache-adapter | drain last phenoShared pin |
| HexaKit | #228 | dependabot/uv/agileplus-mcp/uv-1074aaa37a | uv group bump (UNKNOWN, check first) |
| HeliosLab | #127 | chore/helioslab-next-hygiene-2026-06-14 | cargo-deny all-features |

### T25 — 16 conflicting tooling PRs

PRs #122, #124, #125, #129, #130, #132, #133, #135-#147, #159-#161 (the CONFLICTING ones + group bumps).

### T26 — Mega-PR

PR #122 (`integration/consolidate`).

### T27 — Parent repo push

`KooshaPari/phenotype-apps:main` (currently behind 77, ahead 477 locally).

---

## 6. Risk Register

| Risk | Mitigation |
|---|---|
| PR #122 mega-PR has CI failure | Triage sub-PRs; rebase the high-value 2-3 manually |
| T24 batch merge breaks something | Merge in 3-PR batches (journeys, HexaKit, HeliosLab); check CI after each batch |
| T27 parent repo push conflicts with another session | Use `--force-with-lease` only if origin is our own previous push |
| T29 side-DAG fillers collide with other agents' work | Use dagctl (after T30) or check `claims` table in `FLEET_DAG.db` first |
| T30 dagctl source is gone | Use `phenodag-tool` package or `KooshaPari/phenodag` repo as fallback |

---

## 7. Success Criteria

- [ ] 17+ PRs merged or closed (T24 + T25)
- [ ] 0 open dependabot PRs older than 7 days
- [ ] Parent repo ahead/behind ≤ 10 commits
- [ ] Worktree count ≤ 1 per active repo
- [ ] `dagctl` binary present and tested (T30)
- [ ] v9 side-DAG fillers at 50%+ complete (T29)
- [ ] `findings/2026-06-19-T{24..30}-*.md` exists for each track

---

## 8. Cross-References

- `AGENTS.md` § "Wave Plan" — current state
- `plans/2026-06-19-v9-dag-stable.md` — v9 (T1-T23 governance wave)
- `plans/2026-06-18-v8-dag-stable.md` — v8 (closed)
- `findings/2026-06-19-*.md` — track findings (TBD)
- `FLEET_DAG.db` — shared DAG state (side-NN tasks)
- `dagctl` — Go CLI for shared-lock coordination (T30)

---

## 9. Appendices

### A. Dependabot configuration note

All 14 dependabot PRs are configured with auto-merge. The fleet's dependabot.yml triggers weekly Monday 09:00 PDT per ADR-042 (security cadence). The 14 PRs in this wave likely accumulated between the last auto-merge window and now.

### B. Mega-PR #122 hypothesis

PR #122 (`integration/consolidate`) is suspicious — it claims to "unify all open PR work onto one branch" which is a manual conflict-resolution attempt. If it actually contains the work of the 16 CONFLICTING PRs, closing them as superseded is correct. If it doesn't, #122 should be closed as duplicate work.

### C. Parent repo state interpretation

`main...origin/main [ahead 477, behind 77]` means:
- Local has 477 commits not on origin (the v8/v9 bulk-push work)
- Origin has 77 commits not on local (work pushed by other sessions)

A simple `git pull --rebase` would resolve the behind 77 but create conflicts on the ahead 477. The right move is T27: review the ahead commits, decide which to keep, then either soft-reset + recommit or push to a new branch.

### D. dagctl absence

`dagctl` was removed externally between sessions. The source code may still exist in `phenodag/` or `KooshaPari/phenodag` (the absorption repo). If neither has it, the Go source must be re-collected from git history.