# Finding — task-05-06 — phenotype-registry SSOT.md

- **Task ID:** task-05-06 · **Date:** 2026-06-20 · **Bucket:** HYGIENE · **P1**
- **FLEET_DAG.db state:** DONE (retroactive finding)
- **Repo:** `KooshaPari/phenotype-registry` → `/Users/kooshapari/CodeProjects/Phenotype/repos/phenotype-registry/`

## Repo

phenotype-registry is the **canonical registry of repos, capabilities, and
boundaries** for the Phenotype fleet — the SSOT-of-SSOTs. Located at
`repos/phenotype-registry/` (70 entries, active `.git`).

## Current state

- `docs/SSOT.md` **EXISTS** (100 lines, custom layout — much richer than the
  fleet-standard 33-line skeleton). Front matter declares itself "the single
  source of truth for *what each repo in the fleet does, owns, and does NOT
  own*". Covers Capability & Intent SSOT, canonical artifacts table, cadence,
  audit trail, cross-references.
- State / Dependencies / Architecture sections at the bottom still use the
  fleet-standard placeholder (Hexagonal: in progress).
- Has companion `docs/intent/`, `docs/boundary/`, `docs/registries.md`,
  `ECOSYSTEM_MAP.md`, `_bindings.json`.

## Recommended `docs/SSOT.md` content

**No rewrite needed.** The existing 100-line custom layout IS the canonical
form for registry-style repos. Two refresh items:

1. Update `Last verified: 2026-06-08` → `2026-06-20`.
2. Replace the placeholder Architecture fields at the bottom (lines 78-87)
   with `Hexagonal: yes · Ports: LlmPort, ConfigPort, OtlpPort · Adapters:
   See docs/registries.md §ports` if a hexagonal refactor has landed since
   2026-06-08; otherwise leave the placeholder and note the date.

## Effort

- **Effort:** XS (≤10 min, in-place date bump + Architecture pass-through).
- **Verification:** `wc -l docs/SSOT.md` ≥ 90 (preserve richness); top header
  still says "Capability & Intent SSOT"; `Last verified: 2026-06-20` present.
- **Owner:** worklog-schema circle (registry is owner-of-record for the
  registry SSOT itself).