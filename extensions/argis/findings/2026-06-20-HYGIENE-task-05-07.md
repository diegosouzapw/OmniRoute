# Finding — task-05-07 — FocalPoint SSOT.md

- **Task ID:** task-05-07 · **Date:** 2026-06-20 · **Bucket:** HYGIENE · **P1**
- **FLEET_DAG.db state:** DONE (retroactive finding)
- **Repo:** `KooshaPari/FocalPoint` → `/Users/kooshapari/CodeProjects/Phenotype/repos/FocalPoint/`

## Repo

FocalPoint is a fleet coordination / worktree-container repo and the historical
host of the Phenotype `AGENTS.md` template (2026-06-12 → 2026-06-19). Located
at `repos/FocalPoint/` (466 entries — large, many worktrees). Per the monorepo
AGENTS.md, FocalPoint is now **PAUSED** but referenced as read-only.

## Current state

- `docs/SSOT.md` **EXISTS** (49 lines, custom "Purpose / Scope / Non-Goals /
  Living Document / How to use" layout — a meta-framework document, not the
  standard fleet template).
- The doc frames itself as **canonical reference for architectural decisions**
  rather than a project-state snapshot. Reads more like an ADR preamble.
- State / Dependencies / Architecture sections are absent; the file is
  opinion-driven prose only.

## Recommended `docs/SSOT.md` content

The existing 49-line custom layout is **good** and should be preserved. Two
optional additions (not rewrites):

1. Append a brief `## Fleet Links` section (3 lines) anchoring FocalPoint's
   role: parent=Phenotype, related=`phenotype-monorepo-state` (deleted 2026-06-18
   per ADR-033), contained worktrees include `AtomsBot*`, `phenoForge`.
2. Note in the file that FocalPoint is **PAUSED** as of 2026-06-19; the doc
   remains as historical reference but is not actively maintained.

## Effort

- **Effort:** XS (≤10 min, optional append).
- **Verification:** file still ≥ 49 lines; existing "Purpose / Scope /
  Non-Goals / Living Document / How to use" headings all present.
- **Owner:** none (PAUSED repo). Action optional, not required.