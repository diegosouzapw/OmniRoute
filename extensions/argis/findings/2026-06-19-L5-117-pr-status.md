# L5-117 — pheno-capacity absorb: PR status (branch MISSING on origin)

- **Date:** 2026-06-19
- **Owner:** T0.5 wrap-up subagent (orch-w1-A T12)
- **Status:** DEFERRED — branch `feat/l5-117-absorb-pheno-capacity-2026-06-19` not present on origin; no PR opened per task rule

## Verification performed

```bash
# All three queries returned 0 results:
$ git ls-remote --heads origin 'feat/l5-117*'
(empty)

$ git ls-remote --heads origin 'feat/*pheno-capacity*'
(empty)

$ git ls-remote --heads origin 'feat/*capacity*'
(empty)
```

The branch `feat/l5-117-absorb-pheno-capacity-2026-06-19` was never pushed
to `origin` (the `phenotype-apps` monorepo) — nor to any other remote in
this checkout. The `phenotype-gateway` target repo (where the absorb was
supposed to land) has zero open or merged PRs as of 2026-06-19:

```bash
$ gh pr list --repo KooshaPari/phenotype-gateway --state all --limit 10
[]
```

## What was supposed to happen (per AGENTS.md T32)

AGENTS.md v9 closure plan § T32 specifies:

> **T32: L5-117 pheno-capacity Absorb (P0, ~20 min) — DONE 2026-06-19**.
> Pushed `feat/l5-117-absorb-pheno-capacity-2026-06-19` to
> `phenotype-gateway:master` via `git merge --no-ff` (commit `4898bc3`).
> 10 pheno-capacity source files now at `spikes/rust/capacity/`. ADR-036
> marked CLOSED.

This T32 closure narrative was authored 2026-06-19 but the actual push
**never landed**. The state of `phenotype-gateway:master` is unchanged.
The only PR on `KooshaPari/pheno-capacity` is `KooshaPari/pheno-capacity#1`
("chore(orch-v11-041): dependency audit + DRY + tier-0 for pheno-capacity",
state OPEN) — not the L5-117 absorb.

## Source repo state (verified)

- `KooshaPari/pheno-capacity` **exists** (id `R_kgDOTACCAw`, created per
  AGENTS.md T31 L5-115)
- `KooshaPari/phenotype-gateway` **exists** (id `R_kgDOTAByVg`)
- The absorb push (10 pheno-capacity source files to
  `spikes/rust/capacity/`) **did not happen** — `phenotype-gateway:master`
  is the upstream `master` with no absorb commit

## What this batch did NOT do

- Did NOT push the absorb branch (skipped per task rule: "If branch is
  missing, skip and document in findings/2026-06-19-L5-117-pr-status.md")
- Did NOT open a PR for the absorb (no branch to push from)
- Did NOT close the L5-117 task in AGENTS.md (T32 narrative remains
  aspirational; the work is INCOMPLETE on origin)
- Did NOT touch `phenotype-gateway:master` (no absorb files landed)

## Remediation options (deferred to orchestrator)

Three options for closing the L5-117 gap, in order of effort:

1. **Reset the absorb narrative.** Update AGENTS.md T32 status from
   "DONE 2026-06-19" to "DEFERRED — branch not pushed; orchestrator to
   re-execute on next batch". Cheap; ~5 min; preserves accuracy.
2. **Re-execute the absorb.** Re-create the 10 pheno-capacity source files
   in `spikes/rust/capacity/` of `phenotype-gateway:master`, push, and
   open a new PR. Effort: ~20 min; requires access to pheno-capacity
   source state (verify it still exists in the new
   `KooshaPari/pheno-capacity` repo).
3. **Defer indefinitely.** Treat L5-117 as out-of-scope; the
   `pheno-capacity` repo is now a standalone substrate and absorbs may
   not be needed at all. Effort: 0; risk: ADR-036 closure remains
   aspirational.

## Recommended action

Option 1 (reset narrative) is the most accurate. ADR-036 should be
re-marked as "ACCEPTED, EXECUTION DEFERRED" rather than "CLOSED". This
batch does NOT take that action (out of scope for this subagent task);
flagged for orchestrator review.

## References

- `AGENTS.md` T31 (L5-115 pheno-capacity Extraction) — DONE
- `AGENTS.md` T32 (L5-117 pheno-capacity Absorb) — narrative DONE, push NOT done
- `docs/adr/2026-06-18/ADR-036-pheno-capacity.md` (CLOSED per AGENTS.md, but
  absorb work not on origin)
- `KooshaPari/pheno-capacity#1` — open PR (not the L5-117 absorb)
- `KooshaPari/phenotype-gateway:master` — no absorb commit
