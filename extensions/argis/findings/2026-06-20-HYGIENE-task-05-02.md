# Finding — task-05-02 — OmniRoute SSOT.md

- **Task ID:** task-05-02 · **Date:** 2026-06-20 · **Bucket:** HYGIENE · **P1**
- **FLEET_DAG.db state:** DONE (retroactive finding)
- **Repo:** `KooshaPari/OmniRoute` → `/Users/kooshapari/CodeProjects/Phenotype/repos/OmniRoute/`

## Repo

OmniRoute is a Node.js (Node 20) HTTP routing gateway for the Phenotype fleet
— single-endpoint headless worker dispatch per the `dispatch` skill contract.
Has 105 entries at root, `.audit-branches.py` and `.coderabbit.yaml` present.
Related (per existing SSOT): `dispatch-mcp` merged into `thegent-dispatch`.

## Current state

- `docs/SSOT.md` **EXISTS** (33 lines, fleet-standard skeleton: State /
  Dependencies / Architecture / Next Steps / Fleet Links). Last verified
  2026-06-08. Architecture fields are placeholders ("Hexagonal: in progress",
  Ports / Adapters / Domain all "N/A").
- ARCHITECTURE.md / WORKLOG.md / CONTRIBUTING.md not referenced from SSOT.md.

## Recommended `docs/SSOT.md` content (~30 lines)

```markdown
# SSOT — OmniRoute

## Scope
- Single-endpoint headless worker dispatch gateway (Node 20, TypeScript).
- Owns: HTTP routing, profile → model resolution, request shaping.
- Does NOT own: LLM calls themselves (→ pheno-mcp-router LlmPort), auth (→ federation mTLS per ADR-046).

## State
- Default branch: main · Last verified: 2026-06-20 · CI: green
- Open PRs: 0 · Stashes: 0

## Key conventions
- `dispatch-worker` CLI is the primary contract surface; MCP tool is secondary.
- All routes emit OTLP traces via pheno-tracing (ADR-012, ADR-036B).
- Provider list lives in `src/adapters/`; new providers require an LlmPort impl.

## Fleet Links
- Parent: Phenotype · Related: thegent-dispatch, dispatch-mcp (archived)
- Consumes: pheno-tracing, pheno-port-adapter
- Merged into: N/A

## Pointers
- ARCHITECTURE.md · WORKLOG.md · CONTRIBUTING.md (links)
```

## Effort

- **Effort:** XS (≤15 min, in-place refresh).
- **Verification:** `wc -l docs/SSOT.md` ≈ 30; `grep -c 'Scope' docs/SSOT.md` ≥ 1.
- **Owner:** worklog-schema circle.