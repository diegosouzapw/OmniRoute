# Feature Flags (Русский)

🌐 **Languages:** 🇺🇸 [English](../../../../reference/FEATURE_FLAGS.md) · 🇪🇹 [am](../../../am/docs/reference/FEATURE_FLAGS.md) · 🇸🇦 [ar](../../../ar/docs/reference/FEATURE_FLAGS.md) · 🇦🇿 [az](../../../az/docs/reference/FEATURE_FLAGS.md) · 🇧🇬 [bg](../../../bg/docs/reference/FEATURE_FLAGS.md) · 🇧🇩 [bn](../../../bn/docs/reference/FEATURE_FLAGS.md) · 🇨🇿 [cs](../../../cs/docs/reference/FEATURE_FLAGS.md) · 🇩🇰 [da](../../../da/docs/reference/FEATURE_FLAGS.md) · 🇩🇪 [de](../../../de/docs/reference/FEATURE_FLAGS.md) · 🇬🇷 [el](../../../el/docs/reference/FEATURE_FLAGS.md) · 🇪🇸 [es](../../../es/docs/reference/FEATURE_FLAGS.md) · 🇪🇪 [et](../../../et/docs/reference/FEATURE_FLAGS.md) · 🇮🇷 [fa](../../../fa/docs/reference/FEATURE_FLAGS.md) · 🇫🇮 [fi](../../../fi/docs/reference/FEATURE_FLAGS.md) · 🇫🇷 [fr](../../../fr/docs/reference/FEATURE_FLAGS.md) · 🇮🇪 [ga](../../../ga/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [gu](../../../gu/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ha](../../../ha/docs/reference/FEATURE_FLAGS.md) · 🇮🇱 [he](../../../he/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [hi](../../../hi/docs/reference/FEATURE_FLAGS.md) · 🇭🇷 [hr](../../../hr/docs/reference/FEATURE_FLAGS.md) · 🇭🇺 [hu](../../../hu/docs/reference/FEATURE_FLAGS.md) · 🇦🇲 [hy](../../../hy/docs/reference/FEATURE_FLAGS.md) · 🇮🇩 [id](../../../id/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [ig](../../../ig/docs/reference/FEATURE_FLAGS.md) · 🇮🇹 [it](../../../it/docs/reference/FEATURE_FLAGS.md) · 🇯🇵 [ja](../../../ja/docs/reference/FEATURE_FLAGS.md) · 🇬🇪 [ka](../../../ka/docs/reference/FEATURE_FLAGS.md) · 🇰🇭 [km](../../../km/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [kn](../../../kn/docs/reference/FEATURE_FLAGS.md) · 🇰🇷 [ko](../../../ko/docs/reference/FEATURE_FLAGS.md) · 🇱🇹 [lt](../../../lt/docs/reference/FEATURE_FLAGS.md) · 🇱🇻 [lv](../../../lv/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ml](../../../ml/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [mr](../../../mr/docs/reference/FEATURE_FLAGS.md) · 🇲🇾 [ms](../../../ms/docs/reference/FEATURE_FLAGS.md) · 🇲🇹 [mt](../../../mt/docs/reference/FEATURE_FLAGS.md) · 🇲🇲 [my](../../../my/docs/reference/FEATURE_FLAGS.md) · 🇳🇵 [ne](../../../ne/docs/reference/FEATURE_FLAGS.md) · 🇳🇱 [nl](../../../nl/docs/reference/FEATURE_FLAGS.md) · 🇳🇴 [no](../../../no/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [or](../../../or/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [pa](../../../pa/docs/reference/FEATURE_FLAGS.md) · 🇵🇭 [phi](../../../phi/docs/reference/FEATURE_FLAGS.md) · 🇵🇱 [pl](../../../pl/docs/reference/FEATURE_FLAGS.md) · 🇵🇹 [pt](../../../pt/docs/reference/FEATURE_FLAGS.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/reference/FEATURE_FLAGS.md) · 🇷🇴 [ro](../../../ro/docs/reference/FEATURE_FLAGS.md) · 🇱🇰 [si](../../../si/docs/reference/FEATURE_FLAGS.md) · 🇸🇰 [sk](../../../sk/docs/reference/FEATURE_FLAGS.md) · 🇸🇮 [sl](../../../sl/docs/reference/FEATURE_FLAGS.md) · 🇷🇸 [sr](../../../sr/docs/reference/FEATURE_FLAGS.md) · 🇸🇪 [sv](../../../sv/docs/reference/FEATURE_FLAGS.md) · 🇰🇪 [sw](../../../sw/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [ta](../../../ta/docs/reference/FEATURE_FLAGS.md) · 🇮🇳 [te](../../../te/docs/reference/FEATURE_FLAGS.md) · 🇹🇭 [th](../../../th/docs/reference/FEATURE_FLAGS.md) · 🇹🇷 [tr](../../../tr/docs/reference/FEATURE_FLAGS.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/reference/FEATURE_FLAGS.md) · 🇵🇰 [ur](../../../ur/docs/reference/FEATURE_FLAGS.md) · 🇺🇿 [uz](../../../uz/docs/reference/FEATURE_FLAGS.md) · 🇻🇳 [vi](../../../vi/docs/reference/FEATURE_FLAGS.md) · 🇳🇬 [yo](../../../yo/docs/reference/FEATURE_FLAGS.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/reference/FEATURE_FLAGS.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/reference/FEATURE_FLAGS.md)

---

> Переключатели среды выполнения, которые изменяют поведение OmniRoute **без повторного развертывания**.
> Каждый перечисленный здесь флаг определен в
> [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
> — единственном источнике истины. Панель управления и REST API считывают данные
> из этого файла, поэтому приведенная ниже таблица сформирована в точном соответствии с ним.

---

## Что такое флаги функций

Флаг функции — это именованный переключатель (логический или перечисляемый), значение которого можно изменять
во время выполнения и сохранять в базе данных без необходимости повторного развертывания процесса. Каждый
флаг описывается объектом `FeatureFlagDefinition` с полями `key`, `label`,
`description`, `category`, `defaultValue`, `type` и подсказкой `requiresRestart`.

### Порядок определения значения

**Эффективное значение** флага определяется функцией
[`resolveFeatureFlag()`](../../src/shared/utils/featureFlags.ts) со следующим
приоритетом (побеждает значение с наивысшим приоритетом):

1. **Переопределение в БД** — значение, хранящееся в таблице `key_value` в пространстве имен
   `feature_flags` (задается через панель управления или REST API).
2. **Переменная окружения** — `process.env[<KEY>]`, если она задана и не пуста.
3. **Значение по умолчанию из определения** — `defaultValue` из `featureFlagDefinitions.ts`.

Логический флаг считается **включенным**, если его эффективное значение равно `"true"`,
`"1"` или `"yes"` (см. `isFeatureFlagEnabled()`).

> [!NOTE]
> Большинство флагов также имеют соответствующую переменную окружения **с тем же именем**,
> задокументированную в [`ENVIRONMENT.md`](./ENVIRONMENT.md). Переопределение флага
> в БД имеет приоритет над этой переменной окружения. Флаг с
> `requiresRestart: true` сохраняется немедленно, но повторно считывается только при запуске
> процесса — его переключение приводит к появлению баннера **«Перезапустить сервер»** в панели управления.

---

## Каталог флагов

74 флага в 6 категориях. **По умолчанию** — значение, заданное в определении и используемое,
если отсутствуют как переопределение в БД, так и переменная окружения.

### Безопасность (10)

| Ключ                                    | Тип     | По умолчанию | Описание                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------- | ------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `REQUIRE_API_KEY`                       | boolean | `false`      | Требовать API-ключ для всех входящих запросов.                                                                                                                                                                                                                                                                                                |
| `INPUT_SANITIZER_ENABLED`               | boolean | `true`       | Включить очистку входных данных для всех запросов.                                                                                                                                                                                                                                                                                            |
| `INJECTION_GUARD_MODE`                  | enum    | `off`        | Режим защиты от инъекций в промпты. Значения: `off`, `warn`, `block`, `redact`.                                                                                                                                                                                                                                                               |
| `PII_REDACTION_ENABLED`                 | boolean | `false`      | Редактировать персональные данные в запросах (независимо от `INPUT_SANITIZER_MODE`).                                                                                                                                                                                                                                                          |
| `PII_RESPONSE_SANITIZATION`             | boolean | `false`      | Очищать ответы провайдера от персональных данных.                                                                                                                                                                                                                                                                                             |
| `PII_RESPONSE_SANITIZATION_MODE`        | enum    | `redact`     | Режим очистки ответов от персональных данных. Значения: `redact`, `warn`, `block`, `off`.                                                                                                                                                                                                                                                     |
| `OUTBOUND_SSRF_GUARD_ENABLED`           | boolean | `true`       | Блокировать исходящие запросы к частным/внутренним диапазонам IP-адресов.                                                                                                                                                                                                                                                                     |
| `ALLOW_API_KEY_REVEAL`                  | boolean | `false`      | Разрешить аутентифицированным пользователям панели управления просматривать сохранённые API-ключи вместо отображения только замаскированных значений.                                                                                                                                                                                         |
| `AUTH_LOG_INCLUDE_ACCOUNT_ID`           | boolean | `false`      | Включать префикс учётной записи в строки журнала AUTH (например, «Используется учётная запись <provider>: abc12345...»). По умолчанию отключено, поэтому идентификаторы учётных записей скрываются в общих журналах процессов и журналах многопользовательских сред. Не зависит от режима отладки; его переключение не раскрывает эти данные. |
| `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` | boolean | `false`      | Если OIDC включён, отключить вход по паролю, чтобы пользователи могли проходить аутентификацию только через единый вход OIDC. Если параметр отключён (по умолчанию), доступны как вход по паролю, так и OIDC.                                                                                                                                 |

### Сеть (16)

| Ключ                                            | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------------- | ------- | ------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ENABLE_TLS_FINGERPRINT`                        | boolean | `false`      | ✓          | Включить режим маскировки TLS-отпечатка.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `AUDIO_REMOTE_PROVIDER_NODES`                   | boolean | `false`      |            | Разрешить маршрутам /v1/audio/* использовать совместимые с OpenAI узлы провайдеров, размещённые за пределами localhost. По умолчанию отключено — маршрутизация аудио на удалённый хост изменяет идентификатор исходящего подключения и должна быть явным решением оператора. Узлы loopback разрешены всегда, и этот параметр на них не влияет.                                                                                                                                                                                                                                                                             |
| `RERANK_REMOTE_PROVIDER_NODES`                  | boolean | `false`      |            | Разрешить POST /v1/rerank (и этапу loopback-переранжирования механизма памяти) использовать совместимые с OpenAI узлы провайдеров, размещённые за пределами localhost. По умолчанию отключено — маршрутизация на удалённый хост изменяет идентификатор исходящего подключения и должна быть явным решением оператора. Узлы loopback разрешены всегда; удалённые узлы также должны соответствовать политике исходящих URL провайдера.                                                                                                                                                                                       |
| `PROXY_AUTO_SELECT_ENABLED`                     | boolean | `false`      |            | Если подключению не назначен прокси, автоматически выбирать первый работающий прокси из реестра. По умолчанию отключено (иначе любой прокси из реестра становится глобальным резервным вариантом — #3332).                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_CONTROL_PLANE_PROXY_DIRECT_FALLBACK` | boolean | `false`      |            | Разрешить потокам OAuth и проверки провайдера обходить закреплённый прокси и подключаться напрямую, если предварительные проверки доступности прокси завершаются неудачно. По умолчанию отключено, поскольку это может изменить исходящий IP-адрес.                                                                                                                                                                                                                                                                                                                                                                        |
| `NETWORK_ROTATION_SHARED_EGRESS_GUARD`          | boolean | `true`       |            | При сетевой ошибке (тайм-ауте, отказе или сбросе подключения) в исполнителе ротации с несколькими учётными записями, если у проблемной учётной записи нет выделенного прокси, применить короткий период ожидания и пропустить другие учётные записи без прокси до конца запроса вместо повторной попытки для каждой из них. По умолчанию включено (безопасно: исходящий IP-адрес не изменяется, снижается только риск задержки/периода ожидания для учётных записей с общим исходящим подключением). Отключите, чтобы восстановить немедленное распространение ошибки при первом исключении для учётной записи без прокси. |
| `PROXY_SKIP_RECENTLY_FAILED`                    | boolean | `false`      |            | Пулы прокси и ротация opencode для каждой учётной записи прекращают повторно назначать прокси, на котором только что произошёл сбой (отклонённая проверка TCP или полученный через него ответ 429), на период в рамках процесса, удваивающийся при каждом повторении вплоть до предельного значения. Статус прокси не записывается; если отложены все кандидаты, выбор остаётся неизменным. По умолчанию отключено.                                                                                                                                                                                                        |
| `PROXY_POOL_EGRESS_OBSERVATION`                 | boolean | `false`      |            | Показывать на панели мониторинга для каждого пула прокси, сколько наблюдаемых исходящих IP-адресов обслуживало его участников за последние 24 ч и сколько подключений их использовало. Доступно только для чтения, вычисляется на основе журнала прокси и никогда не используется для маршрутизации. По умолчанию отключено.                                                                                                                                                                                                                                                                                               |
| `OPENCODE_RESPONSES_STALL_ROTATION`             | boolean | `false`      |            | Для исполнителя OpenCode отслеживать первый байт тела потокового ответа Responses (окно: `RESPONSES_FIRST_BYTE_TIMEOUT_MS`, по умолчанию `15000`). Поток Responses с кодом 2xx, который остаётся без данных дольше этого окна, считается зависшим: для учётной записи включается период ожидания, а запрос однократно переключается на следующую учётную запись; при втором зависании запрос немедленно завершается с ошибкой. По умолчанию отключено: зависшие потоки продолжают ожидать до истечения тайм-аута готовности потока.                                                                                        |
| `OPENCODE_USER_BLOCKED_ROTATION`                | boolean | `false`      |            | Исполнитель OpenCode: при ответе 403/451 с отказом `user_blocked` (не связанным с географическими ограничениями или отклонением цифрового отпечатка Cloudflare) временно исключить получившую отказ учётную запись и переключиться на следующую учётную запись не более одного раза на запрос; второй отказ возвращается без изменений и без отметки об успешном выполнении. По умолчанию отключено: обход блокировки пользователя вышестоящим сервисом может выглядеть как уклонение и распространить флаг на весь пул.                                                                                                   |
| `OPENCODE_TRANSIENT_FAILOVER_BACKOFF`           | boolean | `false`      |            | Ротация OpenCode: после двух последовательных временных сбоев вышестоящего сервиса (5xx или пустой ответ 400) приостановить выполнение перед переходом к следующей учётной записи — 1,5 с с удвоением после каждого следующего сбоя, но не более 6 с на паузу и 10 с на запрос; пауза пропускается при отключении клиента, а тело неудачного ответа освобождается до начала ожидания. По умолчанию отключено: переключение при сбое выполняется без задержки.                                                                                                                                                              |
| `OPENCODE_RATE_LIMITED_429_EARLY_STOP`          | boolean | `false`      |            | Ротация OpenCode: остановить перебор учётных записей при первом ответе 429, классифицированном как фактическое ограничение частоты запросов (разбираемый заголовок `Retry-After` или тело ответа с указанием ограничения частоты/использования), и вернуть этот ответ 429 вышестоящего сервиса без изменений. При неклассифицированных ответах 429 ротация продолжается. По умолчанию отключено: бесплатный тариф ограничен на каждый исходящий IP-адрес (#9611), поэтому каждый ответ 429 запускает ротацию, а при исчерпании всех вариантов возвращается последний ответ 429 вышестоящего сервиса.                       |
| `MITM_DISABLE_TLS_VERIFY`                       | boolean | `false`      | ✓          | Отключить проверку сертификатов TLS для MITM-прокси. **Опасно.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS`         | boolean | `false`      |            | Разрешить URL-адреса провайдеров, указывающие на частные/внутренние сети.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_ALLOW_LOCAL_PROVIDER_URLS`           | boolean | `true`       |            | Разрешить добавление/проверку провайдеров по локальным/частным адресам (127.0.0.1, localhost, LAN). Включено по умолчанию (приоритет локальным ресурсам); отключите для строгого блокирования всех адресов, кроме общедоступных. Доступ к облачным метаданным остаётся заблокированным.                                                                                                                                                                                                                                                                                                                                    |
| `ENABLE_CC_COMPATIBLE_PROVIDER`                 | boolean | `false`      | ✓          | Включить режим провайдера, совместимого с Claude Code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

### Политики (5)

| Ключ                            | Тип     | По умолчанию | Описание                                                                                                                                                                                                                                                                |
| ------------------------------- | ------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TOOL_POLICY_MODE`              | enum    | `disabled`   | Режим применения политики использования инструментов. Значения: `disabled`, `warn`, `block`.                                                                                                                                                                            |
| `RATE_LIMIT_AUTO_ENABLE`        | boolean | `false`      | Автоматически включать ограничение частоты запросов на основе шаблонов использования.                                                                                                                                                                                   |
| `DISABLE_CONTEXT_WINDOW_CHECKS` | boolean | `false`      | Пропустить локальную проверку окна контекста / максимального количества входных токенов в OmniRoute для прямых запросов к одной модели. Ограничения вышестоящего сервиса по-прежнему применяются.                                                                       |
| `CAPABILITY_FILTER_ENABLED`     | boolean | `false`      | Отклонять запросы до отправки, если целевая модель не поддерживает необходимые возможности (изображения, инструменты, структурированный вывод, окно контекста). Защищает прямые запросы к одному провайдеру, которые обходят фильтр совместимости на уровне комбинаций. |
| `RADAR_ENABLED`                 | boolean | `false`      | Включить модуль OmniRoute Radar (экраны каталога и синхронизации каналов данных). По умолчанию отключено; включение лишь открывает интерфейс — синхронизация данных требует отдельного явного согласия.                                                                 |

### Среда выполнения (33)

| Ключ                                        | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------- | ------- | ------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `UNIVERSAL_CONTEXT_HANDOFF_ENABLED`         | boolean | `true`       |            | Создавать и внедрять сводки беседы при переключении моделей комбинированной маршрутизацией. Отключите, чтобы обрабатывать переключения моделей независимо и запретить фоновые запросы передачи контекста для всех существующих и будущих комбинаций.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `RESPONSES_PASSTHROUGH_DROP_COMMENTARY`     | boolean | `true`       |            | Удалять внутренние элементы вывода этапа комментариев из сквозных потоков Responses API перед отправкой клиентам. Отключите, чтобы получать необработанные комментарии от вышестоящего сервиса.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `OMNIROUTE_MCP_ENFORCE_SCOPES`              | boolean | `true`       |            | Применять ограничения областей доступа к инструментам MCP.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `OMNIROUTE_MCP_COMPRESS_DESCRIPTIONS`       | boolean | `false`      |            | Сжимать описания инструментов MCP для сокращения использования токенов.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_ENABLE_RUNTIME_BACKGROUND_TASKS` | boolean | `false`      |            | Включить обработку фоновых задач во время выполнения.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `OMNIROUTE_DISABLE_BACKGROUND_SERVICES`     | boolean | `false`      | ✓          | Отключить все фоновые службы (обновление квот, синхронизацию и т. д.).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_RTK_TRUST_PROJECT_FILTERS`       | boolean | `false`      |            | Доверять фильтрам RTK уровня проекта без проверки.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `OMNIROUTE_ENABLE_LIVE_WS`                  | boolean | `true`       | ✓          | Запускать при импорте WebSocket-сервер панели мониторинга в реальном времени (по умолчанию порт 20132).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_CODEX_WS_ENABLED`                | boolean | `true`       |            | Разрешить Codex использовать транспорт Responses-over-WebSocket. Если отключено, Codex переключается на HTTP Responses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `OMNIROUTE_CODEX_APP_SERVER_ENABLED`        | boolean | `true`       |            | Разрешить Codex использовать локальный транспорт app-server WebSocket JSON-RPC (codexTransport=app-server). Если отключено, соединения, настроенные на app-server, переключаются на другие транспорты Codex.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `OMNIROUTE_EMERGENCY_FALLBACK`              | boolean | `true`       |            | Перенаправлять запросы с исчерпанным бюджетом к аварийному бесплатному резервному провайдеру/модели. (См. раздел [Аварийное переключение при исчерпании бюджета](#emergency-budget-fallback) ниже.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `STREAM_RECOVERY_ENABLED`                   | boolean | `false`      |            | Включить прозрачную раннюю повторную попытку для усечённых вышестоящих потоков SSE до того, как клиенту будут переданы какие-либо байты ответа.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `STREAM_RECOVERY_MIDSTREAM_ENABLED`         | boolean | `false`      |            | Разрешить восстановлению потока повторно запросить и объединить ответ после того, как байты уже были переданы клиенту.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `STREAM_RECOVERY_TOOLCALL_ORDER_FIX`        | boolean | `false`      |            | Сделать продолжение в середине потока безопасным для вызовов инструментов: никогда не возобновлять прерванный поток после отправки вызова инструмента (выполняющегося или уже завершённого с finish_reason tool_calls) и закрывать поток после одного пустого продолжения вместо расходования всего бюджета. Если отключено: стандартное поведение релиза.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `STREAM_EARLY_EOF_SIBLING_FAILOVER_ENABLED` | boolean | `false`      |            | Однократно переключаться на родственное соединение, если поток SSE закрывается до отправки какого-либо полезного фрейма и ограниченное число повторных попыток в том же соединении исчерпано; если подходящего родственного соединения нет, возвращается исходная ошибка `STREAM_EARLY_EOF` 502. По умолчанию отключено: ранний EOF остаётся терминальным после повторной попытки в том же соединении.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `MODEL_CATALOG_INCLUDE_NAMES`               | boolean | `true`       |            | Включать удобные для отображения поля имён в ответы `/v1/models`. Отключите для клиентов, которым нужны только идентификаторы моделей.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `MODELS_CATALOG_PREFIX_MODE`                | enum    | `dual`       |            | Управляет добавлением префиксов к идентификаторам моделей в /v1/models. Режим 'dual' (по умолчанию) возвращает префиксы как псевдонима, так и канонического идентификатора провайдера для обратной совместимости. Режим 'alias' возвращает только короткий префикс псевдонима (например, ds-web/model, а не deepseek-web/model). Режим 'canonical' возвращает только полный префикс идентификатора провайдера. Значения: `dual`, `alias`, `canonical`.                                                                                                                                                                                                                                                                                                                                                 |
| `ARENA_ELO_SYNC_ENABLED`                    | boolean | `true`       |            | Включить периодическую синхронизацию ELO из таблицы лидеров Arena AI для рейтингов интеллектуальных возможностей моделей.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `EXPOSE_CC_DISCOVERY_ALIASES`               | boolean | `false`      |            | Публиковать зеркальные идентификаторы `claude/<provider>/<model>` в `/v1/models`, чтобы обнаружение моделей шлюзом Claude Code отображало модели, отличные от Claude. Глобальный уровень трёхуровневого механизма управления (переменная окружения имеет приоритет над переопределением в панели управления). См. [конфигурацию Claude Code](../guides/CLAUDE-CODE-CONFIGURATION.md#discovery-aliases--surface-non-claude-models-in-the-model-picker).                                                                                                                                                                                                                                                                                                                                                 |
| `NO_THINKING_ALIAS_ENABLED`                 | boolean | `true`       |            | Главный переключатель псевдонимов шлюза no-think/<provider>/<model>. Включено (по умолчанию): /v1/models публикует вариант без размышления для каждой подходящей модели Claude с поддержкой размышления, а идентификатор no-think/, переданный в запросе, сопоставляется с реальной моделью с отключёнными рассуждениями. Отключено: варианты не публикуются, а идентификатор no-think/ обрабатывается как любой другой неизвестный идентификатор модели. Пока эта функция включена, по-прежнему применяется индивидуальная для каждой модели настройка включения или отключения ModelSpec.noThinkingAlias.                                                                                                                                                                                            |
| `OMNIROUTE_DISABLE_THINKING_LEVEL_VARIANTS` | boolean | `false`      |            | Отключить создание вариантов уровней размышления (например, -low, -medium, -high) в каталоге /v1/models.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `OMNIROUTE_CHAT_VIRTUAL_LANES`              | boolean | `false`      | ✓          | Включить адаптивные виртуальные полосы допуска для каждого арендатора при диспетчеризации провайдеру (#9654): всплеск нагрузки одного арендатора больше не приводит к ошибкам 503 у другого. Переменная окружения OMNIROUTE_CHAT_VIRTUAL_LANES имеет приоритет над этим переопределением в панели управления; изменения вступают в силу после перезапуска сервера.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `EXPOSE_FUNCTIONAL_GATEWAY_MIRRORS`         | boolean | `false`      |            | Публиковать идентификаторы зеркал <gateway-alias>/<model> в /v1/models для моделей, у канонического владельца которых нет активных учетных данных, но которые маршрутизируются транзитным шлюзом с активными учетными данными. Предупреждение: при глобальном включении добавляет записи в каталог для всех клиентов.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `NEWAPI_AGGREGATOR_BALANCE`                 | boolean | `false`      |            | Включить определение баланса для узлов-агрегаторов, совместимых с New-API / One-API / Sub2API. Если этот параметр включен, совместимые узлы с установленным флагом агрегатора будут отображать свой баланс на панели управления и использовать его при предварительной проверке квоты во время маршрутизации.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `SERVER_OWNED_TOOL_LOOP_ENABLED`            | boolean | `false`      |            | Продолжать принадлежащие серверу вызовы инструментов без потоковой передачи, пока модель не вернет ответ, пригодный для использования клиентом.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `SEARCH_STATS_HIDE_DELETED_CONNECTIONS`     | boolean | `false`      |            | В статистике поиска и недавних поисковых запросах учитывать только поставщиков, у которых по-прежнему есть активное подключение (поставщики без ключей, например duckduckgo-free, учитываются всегда). Если параметр выключен, сохраняются все оставшиеся строки поиска с идентификатором поставщика.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `FREE_BADGE_REQUIRES_PROVIDER_FREE_TIER`    | boolean | `false`      |            | Страницы поставщиков на панели управления: показывать значок «Бесплатно» только при наличии подтвержденных поставщиком признаков — не использовать эвристику по отображаемому имени, небулевы поля бесплатности и суффиксы :free у зарегистрированных поставщиков без документально подтвержденного бесплатного тарифа. Если параметр выключен, сохраняется прежнее правило отображения значка.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `RETRY_AFTER_PROVENANCE_ENABLED`            | boolean | `false`      |            | В агрегированных ответах 429/503 о недоступности не включать `Retry-After`, если конкретное будущее время повторной попытки неизвестно (вместо искусственного значения 1 с), добавлять `error.retry_after_provenance` (`signal` \| `none`) и разрешить путям исчерпания комбинации считывать текстовые подсказки о повторной попытке из JSON и текстовых тел ответов вышестоящих сервисов. Поле появляется только в ответах, созданных `unavailableResponse()`; остальные тела ответов 429/503 не изменяются.                                                                                                                                                                                                                                                                                          |
| `PROTECTED_PRIORITY_INFRA_502_ENABLED`      | boolean | `false`      |            | Если целевой элемент комбинации `priority`, помеченный как резервный только при исчерпании квоты, останавливает комбинацию по причине, которая заведомо не связана с квотой (разомкнут автоматический выключатель поставщика, пропуск из-за прогнозируемой задержки), отвечать кодом 502 вместо кода 503, выглядящего как ошибка квоты. При блокировке, периоде ожидания, недоступности, исчерпании ресурсов и остановке из-за ограничения параллелизма сохраняется код 503.                                                                                                                                                                                                                                                                                                                           |
| `MISTRAL_AMBIGUOUS_401_SOFT_LOCKOUT`        | boolean | `false`      |            | Простой ответ Mistral 401 (`{"detail":"Unauthorized"}` без явного признака ошибки аутентификации) выглядит одинаково и для отозванного ключа, и для исчерпанной квоты. Если параметр включен, для подключения активируется период ожидания вместо его перевода в состояние `expired`, но не более 3 раз в час для каждого подключения; при следующем таком ответе подключение переводится в это состояние, поэтому отозванный ключ в итоге все равно будет выявлен. По умолчанию параметр выключен: каждый простой ответ Mistral 401, как и прежде, переводит подключение в состояние `expired`.                                                                                                                                                                                                       |
| `XAI_OAUTH_LIVE_MODEL_DISCOVERY`            | boolean | `false`      |            | Получать актуальный каталог моделей xAI для подключений `xai-oauth` из `https://api.x.ai/v1/models`, используя OAuth bearer-токен, вместо зафиксированного статического начального набора. По умолчанию отключено: `xai-oauth` продолжает предоставлять статический начальный набор без изменений. При любой ошибке разрешения обнаружение возвращается к начальному набору (не проверено, принимает ли x.ai OAuth bearer-токен для этой конечной точки).                                                                                                                                                                                                                                                                                                                                              |
| `BATCH_AND_FILE_AUTO_CLEANUP_ENABLED`       | boolean | `false`      |            | Разрешить автоматической процедуре очистки удалять завершённые (completed/failed/cancelled/expired) задания Batch API старше `OMNIROUTE_BATCH_RETENTION_DAYS` вместе с их контрольными точками для каждой строки, а также очищать BLOB-содержимое загруженных файлов после наступления их собственного `expires_at`. По умолчанию отключено: каждая существующая установка продолжает хранить эти данные в прежнем виде, пока оператор явно не включит эту возможность. Маршрут `DELETE /api/v1/batches/delete-completed`, запускаемый оператором, в любом случае не затрагивается — это отдельный безусловный контракт публичного API.                                                                                                                                                                |
| `ANTIGRAVITY_ACCOUNT_LEASE_ENABLED`         | boolean | `false`      |            | Резервировать выбранную учётную запись Antigravity на время жизненного цикла потоковой передачи запроса, который её выбрал, чтобы параллельная повторная попытка или передача учётных данных не могли повторно выбрать учётную запись, уже задействованную в активном потоке. Резервирование ограничено парой (подключение, вызываемая вышестоящая модель), поэтому одна учётная запись по-прежнему может одновременно обслуживать две разные модели. Если все подходящие учётные записи уже зарезервированы для этой модели, запрос возвращает структурированную ошибку 503 `antigravity_pool_busy` с ограниченным значением `Retry-After` вместо перенаправления нагрузки на занятую учётную запись. По умолчанию отключено: выбор учётной записи остаётся прежним, и резервирование не выполняется. |

### CLI (5)

| Ключ                                  | Тип     | По умолчанию | Перезапуск | Описание                                                                                                                                                                                                                                                                         |
| ------------------------------------- | ------- | ------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CLI_COMPAT_ALL`                      | boolean | `false`      | ✓          | Включить режим совместимости для всех клиентов CLI.                                                                                                                                                                                                                              |
| `MODEL_ALIAS_COMPAT_ENABLED`          | boolean | `false`      |            | Включить уровень совместимости псевдонимов моделей.                                                                                                                                                                                                                              |
| `PRICING_SYNC_ENABLED`                | boolean | `false`      |            | Включить автоматическую синхронизацию данных о ценах (также требуется переменная окружения `PRICING_SYNC_ENABLED`).                                                                                                                                                              |
| `OMNIROUTE_AUTO_SYNC_CODEX_PROFILES`  | boolean | `false`      |            | После синхронизации моделей провайдера автоматически (пере)записывать файлы профилей ~/.codex/*.config.toml на основе актуального каталога. Никогда не изменяет активную конфигурацию Codex или конфигурацию Codex по умолчанию. По умолчанию отключено.                         |
| `OMNIROUTE_AUTO_SYNC_CLAUDE_PROFILES` | boolean | `false`      |            | После синхронизации моделей провайдера автоматически (пере)записывать профили Claude Code ~/.claude/profiles/<name>/settings.json на основе актуального каталога. Никогда не изменяет активную конфигурацию Claude или конфигурацию Claude по умолчанию. По умолчанию отключено. |

### Работоспособность (5)

| Ключ                                      | Тип     | По умолчанию | Описание                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------------------- | ------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `OMNIROUTE_DISABLE_LOCAL_HEALTHCHECK`     | boolean | `false`      | Отключает конечную точку проверки работоспособности локального экземпляра.                                                                                                                                                                                                                                                                 |
| `OMNIROUTE_DISABLE_TOKEN_HEALTHCHECK`     | boolean | `false`      | Отключает проверку работоспособности для валидации токена.                                                                                                                                                                                                                                                                                 |
| `SKILLS_SANDBOX_NETWORK_ENABLED`          | boolean | `false`      | Разрешает доступ к сети в изолированной среде навыков.                                                                                                                                                                                                                                                                                     |
| `PROXY_HEALTH_BLOCKED_RESETS_STREAK`      | boolean | `false`      | При проверке работоспособности прокси отклонённый целевым сервером запрос (401/403/429) сбрасывает счётчик последовательных сбоев прокси. По умолчанию отключено: отказ остаётся нейтральным (#10654). Ответ 5xx в любом случае считается неопределённым; отказ никогда не приводит к удалению, отключению или повторной активации прокси. |
| `DB_HEALTHCHECK_STARTUP_DEFERRED_ENABLED` | boolean | `false`      | Выполняет проверку целостности и работоспособности БД при запуске после того, как сервер начинает принимать запросы (через `setImmediate`), вместо блокировки запуска до её завершения (#13717). По умолчанию отключено: запуск блокируется точно так же, как и до этого PR.                                                               |

> [!NOTE]
> `INPUT_SANITIZER_BLOCK_THRESHOLD` и его устаревший псевдоним
> `INJECTION_GUARD_BLOCK_THRESHOLD` настраивают режим `block` для
> `INJECTION_GUARD_MODE`, но являются обычными переменными среды, считываемыми
> [`src/shared/utils/injectionSeverity.ts`](../../src/shared/utils/injectionSeverity.ts),
> а не флагами функций: для них нельзя переопределить значение через БД, и у них нет переключателя на панели управления. См.
> [`ENVIRONMENT.md`](./ENVIRONMENT.md#4-security--authentication).

> [!NOTE]
> Столбец `Restart` отмечает флаги с `requiresRestart: true` — значение
> сохраняется мгновенно, но вступает в силу только после перезапуска процесса. Флаги-перечисления
> отклоняют любые значения вне разрешённого набора (валидация выполняется на стороне сервера
> как в `setFeatureFlagOverride()`, так и в обработчике REST `PUT`).

---

## Переключение флагов

### Панель управления

Перейдите в раздел **Панель управления → Настройки → Флаги функций**
(`/dashboard/settings/feature-flags`). Таблица
(`src/app/(dashboard)/dashboard/settings/components/FeatureFlagsGrid.tsx`)
поддерживает:

- **Поиск** по ключу или описанию и **фильтрацию** по категории (а также синтетическое
  представление **Требуется перезапуск**).
- **Переключатель** для булевых флагов и **выпадающий список** для флагов-перечислений
  (`src/app/(dashboard)/dashboard/settings/components/FeatureFlagCard.tsx`).
- **Метка источника** для каждого флага — `DB`, `ENV` или `DEF`, — показывающая, откуда
  получено действующее значение.
- Кнопка **Сбросить** (отображается только для флагов с источником `DB`) для удаления переопределения
  и кнопка **Сбросить все переопределения** в нижней части страницы.
- Баннер **Перезапустить сервер**, когда изменяется флаг `requiresRestart`.

### REST API

Все операции выполняются через единый маршрут:
[`src/app/api/settings/feature-flags/route.ts`](../../src/app/api/settings/feature-flags/route.ts).
Каждый метод требует аутентифицированного сеанса панели управления (иначе возвращается `401`).

#### `GET /api/settings/feature-flags`

Возвращает все флаги с их действующими значениями, источниками и сводной информацией.

```jsonc
{
  "flags": [
    {
      "key": "REQUIRE_API_KEY",
      "label": "Требовать ключ API",
      "description": "Требовать ключ API для всех входящих запросов",
      "category": "security",
      "type": "boolean",
      "enumValues": null,
      "defaultValue": "false",
      "effectiveValue": "false",
      "source": "default", // "db" | "env" | "default"
      "requiresRestart": false,
      "warningLevel": "caution",
    },
    // ... все 74 флага
  ],
  "summary": {
    "total": 56,
    "active": 0,
    "inactive": 0,
    "overriddenByDb": 0,
    "overriddenByEnv": 0,
  },
}
```

#### `PUT /api/settings/feature-flags`

Устанавливает или удаляет одно переопределение. Тело запроса: `{ key: string; value?: string }`.
Если `value` не указан, переопределение удаляется (восстанавливается значение из окружения / значение по умолчанию).

```bash
# Установить переопределение в БД
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY","value":"true"}'

# Удалить переопределение (без "value")
curl -X PUT http://localhost:20128/api/settings/feature-flags \
  -H "Content-Type: application/json" \
  -d '{"key":"REQUIRE_API_KEY"}'
```

Ответ содержит новые значения `effectiveValue`/`source`, предыдущие значения `previousValue`/
`previousSource` и `requiresRestart`. Неизвестные ключи и недопустимые значения перечислений
отклоняются с кодом `400`.

#### `DELETE /api/settings/feature-flags`

Удаляет **все** переопределения из БД одновременно, восстанавливая для каждого флага значение из окружения / значение по умолчанию.
Возвращает `{ cleared: <count>, message: "..." }`.

> [!NOTE]
> Флаги с `requiresRestart: true` вступают в силу только после перезапуска процесса.
> Механизм перезапуска в панели управления вызывает `POST /api/restart`, а затем опрашивает
> `GET /api/health/ping`, пока сервер снова не станет доступен.

---

## Аварийный резерв при исчерпании бюджета

`OMNIROUTE_EMERGENCY_FALLBACK` (категория `runtime`, значение по умолчанию `true`) управляет
аварийным путём бесплатного резервирования в
[`open-sse/services/emergencyFallback.ts`](../../open-sse/services/emergencyFallback.ts).
Когда он включён, запросы, исчерпавшие свой бюджет, направляются к бесплатному резервному
провайдеру/модели вместо немедленного завершения с ошибкой. Установите значение `false` (или `0`) — через
переключатель на панели управления, переопределение в БД или переменную окружения
`OMNIROUTE_EMERGENCY_FALLBACK` — чтобы отключить это поведение и позволить запросам с исчерпанным
бюджетом завершаться с ошибкой. (Добавлено как переключатель на панели управления в PR #3741 / #3752.)

---

## См. также

- [Справочник переменных окружения](./ENVIRONMENT.md) — для большинства флагов
  там задокументирована одноимённая переменная окружения (переопределение в БД
  имеет над ней приоритет).
- [`src/shared/constants/featureFlagDefinitions.ts`](../../src/shared/constants/featureFlagDefinitions.ts)
  — единый источник достоверной информации обо всех флагах.
- [`src/shared/utils/featureFlags.ts`](../../src/shared/utils/featureFlags.ts)
  — логика разрешения (`resolveFeatureFlag`, `isFeatureFlagEnabled`,
  `resolveAllFeatureFlags`).
- [`src/lib/db/featureFlags.ts`](../../src/lib/db/featureFlags.ts) — хранение
  переопределений БД в пространстве имён `feature_flags` таблицы `key_value`.
