# 🗜️ Prompt Compression Guide — OmniRoute (العربية)

🌐 **Languages:** 🇺🇸 [English](../../../../compression/COMPRESSION_GUIDE.md) · 🇪🇹 [am](../../../am/docs/compression/COMPRESSION_GUIDE.md) · 🇦🇿 [az](../../../az/docs/compression/COMPRESSION_GUIDE.md) · 🇧🇬 [bg](../../../bg/docs/compression/COMPRESSION_GUIDE.md) · 🇧🇩 [bn](../../../bn/docs/compression/COMPRESSION_GUIDE.md) · 🇧🇦 [bs](../../../bs/docs/compression/COMPRESSION_GUIDE.md) · 🇨🇿 [cs](../../../cs/docs/compression/COMPRESSION_GUIDE.md) · 🇩🇰 [da](../../../da/docs/compression/COMPRESSION_GUIDE.md) · 🇩🇪 [de](../../../de/docs/compression/COMPRESSION_GUIDE.md) · 🇬🇷 [el](../../../el/docs/compression/COMPRESSION_GUIDE.md) · 🇪🇸 [es](../../../es/docs/compression/COMPRESSION_GUIDE.md) · 🇪🇪 [et](../../../et/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇷 [fa](../../../fa/docs/compression/COMPRESSION_GUIDE.md) · 🇫🇮 [fi](../../../fi/docs/compression/COMPRESSION_GUIDE.md) · 🇫🇷 [fr](../../../fr/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇪 [ga](../../../ga/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [gu](../../../gu/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇬 [ha](../../../ha/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇱 [he](../../../he/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [hi](../../../hi/docs/compression/COMPRESSION_GUIDE.md) · 🇭🇷 [hr](../../../hr/docs/compression/COMPRESSION_GUIDE.md) · 🇭🇺 [hu](../../../hu/docs/compression/COMPRESSION_GUIDE.md) · 🇦🇲 [hy](../../../hy/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇩 [id](../../../id/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇬 [ig](../../../ig/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇹 [it](../../../it/docs/compression/COMPRESSION_GUIDE.md) · 🇯🇵 [ja](../../../ja/docs/compression/COMPRESSION_GUIDE.md) · 🇬🇪 [ka](../../../ka/docs/compression/COMPRESSION_GUIDE.md) · 🇰🇭 [km](../../../km/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [kn](../../../kn/docs/compression/COMPRESSION_GUIDE.md) · 🇰🇷 [ko](../../../ko/docs/compression/COMPRESSION_GUIDE.md) · 🇱🇹 [lt](../../../lt/docs/compression/COMPRESSION_GUIDE.md) · 🇱🇻 [lv](../../../lv/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [ml](../../../ml/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [mr](../../../mr/docs/compression/COMPRESSION_GUIDE.md) · 🇲🇾 [ms](../../../ms/docs/compression/COMPRESSION_GUIDE.md) · 🇲🇹 [mt](../../../mt/docs/compression/COMPRESSION_GUIDE.md) · 🇲🇲 [my](../../../my/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇵 [ne](../../../ne/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇱 [nl](../../../nl/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇴 [no](../../../no/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [or](../../../or/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [pa](../../../pa/docs/compression/COMPRESSION_GUIDE.md) · 🇵🇭 [phi](../../../phi/docs/compression/COMPRESSION_GUIDE.md) · 🇵🇱 [pl](../../../pl/docs/compression/COMPRESSION_GUIDE.md) · 🇵🇹 [pt](../../../pt/docs/compression/COMPRESSION_GUIDE.md) · 🇧🇷 [pt-BR](../../../pt-BR/docs/compression/COMPRESSION_GUIDE.md) · 🇷🇴 [ro](../../../ro/docs/compression/COMPRESSION_GUIDE.md) · 🇷🇺 [ru](../../../ru/docs/compression/COMPRESSION_GUIDE.md) · 🇱🇰 [si](../../../si/docs/compression/COMPRESSION_GUIDE.md) · 🇸🇰 [sk](../../../sk/docs/compression/COMPRESSION_GUIDE.md) · 🇸🇮 [sl](../../../sl/docs/compression/COMPRESSION_GUIDE.md) · 🇷🇸 [sr](../../../sr/docs/compression/COMPRESSION_GUIDE.md) · 🇸🇪 [sv](../../../sv/docs/compression/COMPRESSION_GUIDE.md) · 🇰🇪 [sw](../../../sw/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [ta](../../../ta/docs/compression/COMPRESSION_GUIDE.md) · 🇮🇳 [te](../../../te/docs/compression/COMPRESSION_GUIDE.md) · 🇹🇭 [th](../../../th/docs/compression/COMPRESSION_GUIDE.md) · 🇹🇷 [tr](../../../tr/docs/compression/COMPRESSION_GUIDE.md) · 🇺🇦 [uk-UA](../../../uk-UA/docs/compression/COMPRESSION_GUIDE.md) · 🇵🇰 [ur](../../../ur/docs/compression/COMPRESSION_GUIDE.md) · 🇺🇿 [uz](../../../uz/docs/compression/COMPRESSION_GUIDE.md) · 🇻🇳 [vi](../../../vi/docs/compression/COMPRESSION_GUIDE.md) · 🇳🇬 [yo](../../../yo/docs/compression/COMPRESSION_GUIDE.md) · 🇨🇳 [zh-CN](../../../zh-CN/docs/compression/COMPRESSION_GUIDE.md) · 🇹🇼 [zh-TW](../../../zh-TW/docs/compression/COMPRESSION_GUIDE.md)

---

> وفّر تلقائيًا من 15% إلى 95% من السياق المؤهل. للحصول على نظرة عامة سريعة، راجع [قسم الضغط في README](../README.md#%EF%B8%8F-prompt-compression--save-15-95-eligible-tokens-automatically).

## نظرة عامة

ينفّذ OmniRoute مسارًا معياريًا لضغط المطالبات يعمل **استباقيًا** قبل وصول الطلبات إلى موفّري الخدمة في المنبع. وهذا يعني أن توفير الرموز يتم بشفافية — من دون الحاجة إلى إجراء أي تغييرات على سير عملك.

```
طلب العميل
  → محدد استراتيجية الضغط
    → هل يوجد تجاوز عبر مجموعة؟ → استخدام إعداد المجموعة
    → هل تم بلوغ حد التشغيل التلقائي؟ → استخدام الوضع التلقائي
    → هل يوجد وضع افتراضي؟ → استخدام الإعداد العام
    → هل الضغط متوقف؟ → تخطي الضغط
  → وضع الضغط المحدد
    → متوقف: دون ضغط
    → خفيف: تنظيف آمن للمسافات البيضاء/التنسيق (~15%)
    → قياسي: إزالة الحشو بأسلوب الكلام المقتضب (~30%)
    → مكثف: تقادم السجل + التلخيص (~50%)
    → فائق: تقليم استدلالي + تقليص كتل التعليمات البرمجية (~75%)
    → RTK: تصفية واعية بالأوامر لمخرجات الطرفية/الأدوات (نطاق 60-90% في المنبع)
    → مكدّس: مسار مرتب متعدد المحركات، عادةً RTK ثم Caveman (نطاق 78-95% للمحتوى المؤهل)
  → الطلب المضغوط → الموفّر
```

---

## أوضاع الضغط

### متوقف

لا يُطبّق أي ضغط. تمر جميع الرسائل من دون تغيير.

### الوضع الخفيف (توفير ~15%، وزمن استجابة <1ms)

الوضع الأكثر أمانًا — لا تغيير دلاليًا، بل تنظيف للتنسيق فقط:

| التقنية                  | الوصف                                          |
| ------------------------ | ---------------------------------------------- |
| `collapseWhitespace`     | دمج الأسطر الفارغة المتتالية والمسافات اللاحقة |
| `dedupSystemPrompt`      | إزالة رسائل النظام المكررة                     |
| `compressToolResults`    | ضغط مخرجات الأدوات/الدوال المطوّلة             |
| `removeRedundantContent` | حذف التعليمات المتكررة                         |
| `replaceImageUrls`       | تقصير عناوين URI لبيانات الصور بترميز base64   |

**الأنسب لـ:** الاستخدام الدائم، وسير العمل الحساس للسلامة.

### الوضع القياسي (توفير ~30%)

مستوحى من [Caveman](https://github.com/JuliusBrussee/caveman) — يزيل الكلمات الحشوية والصياغات المطوّلة مع الحفاظ على المعنى:

- يزيل الكلمات الحشوية ("من فضلك"، "أعتقد"، "بشكل أساسي"، "في الواقع")
- يختصر العبارات المطوّلة ("من أجل" ← "لـ"، "نتيجةً لـ" ← "بسبب")
- يزيل عبارات التلطيف المهذبة ("هل تمانع..."، "إذا كان بإمكانك...")
- أكثر من 30 قاعدة regex مضبوطة لمطالبات البرمجة

**الأنسب لـ:** سير عمل البرمجة اليومي، والفرق الحريصة على التكاليف.

### الوضع المكثف (توفير ~50%)

إدارة ذكية للسجل في الجلسات الطويلة:

- **تقادم الرسائل** — تُضغط الرسائل الأقدم تدريجيًا
- **تلخيص نتائج الأدوات** — تُستبدل مخرجات الأدوات الطويلة بملخصات
- **ضوابط السلامة الهيكلية** — تضمن بقاء أزواج `tool_use` و`tool_result` متسقة
- **مراعاة نافذة السياق** — يلتزم بحدود الرموز الخاصة بكل نموذج

**الأنسب لـ:** جلسات تصحيح الأخطاء المطوّلة، وقواعد التعليمات البرمجية الكبيرة.

### الوضع الفائق (توفير ~75%)

أقصى ضغط للسيناريوهات الحرجة من حيث الرموز:

- **التقليم الاستدلالي** — يزيل الرسائل التي تقل عن عتبة الصلة
- **تقليص كتل التعليمات البرمجية** — يضغط أمثلة التعليمات البرمجية المتكررة
- **الاقتطاع بالبحث الثنائي** — يعثر على نقطة القطع المثلى لنافذة السياق
- يتضمن جميع ميزات الوضع المكثف

**الأنسب لـ:** الحالات التي تبلغ فيها حدود السياق بصورة متكررة.

### وضع RTK (نطاق 60-90% في المنبع)

تم تحسين وضع RTK لمخرجات الأدوات المطوّلة التي تظهر في جلسات وكلاء البرمجة:

- يكتشف فئات الأوامر/المخرجات مثل `git status` و`git diff` و`git log` وأدوات تشغيل الاختبارات،
  وعمليات بناء TypeScript/Vite/Webpack، وESLint/Biome/Prettier، وعمليات تدقيق/تثبيت npm، وسجلات Docker، ومخرجات
  البنية التحتية، ومخرجات الصدفة العامة
- يطبّق حزم مرشحات JSON من `open-sse/services/compression/engines/rtk/filters/`
- يستورد مرشحات مخطط RTK TOML بالإصدار v1 من ملفات `filters.toml` الخاصة بالمشروع أو العامة، مع التحقق
  بواسطة الاختبارات المضمّنة والتحكم القائم على الثقة لملفات المشروع
- يتضمن 49 مرشحًا مدمجًا مع عينات تحقق مضمّنة
- يزيل تسلسلات تحكم ANSI وأشرطة التقدم والأسطر المتكررة والضوضاء غير القابلة لاتخاذ إجراء
- يحافظ على حالات الفشل والأخطاء والتحذيرات والملفات المتغيرة والملخصات ونهاية المخرجات الطويلة
- يدعم مرشحات المشروع الخاضعة للتحكم القائم على الثقة، والمرشحات العامة، والاستعادة الاختيارية للمخرجات الخام المنقّحة

**الأنسب لـ:** جلسات الوكلاء التي تتضمن نصوصًا منقولة من الصدفة وعمليات البناء والاختبار وgit وgrep ومخرجات الملفات.

### الوضع المكدّس (نطاق 78-95% للمحتوى المؤهل)

يشغّل الوضع المكدّس عدة محركات ضغط بترتيب حتمي. المسار الافتراضي هو:

```txt
RTK -> Caveman
```

يحافظ هذا الترتيب أولًا على إيجاز مخرجات الطرفية/الأدوات، ثم يطبّق التكثيف الدلالي لـ Caveman على
ما تبقى من مطالبة اللغة الطبيعية. يمكن تكوين المسارات المكدّسة بشكل عام أو من خلال
مجموعات الضغط المعيّنة لمجموعات التوجيه.

**الأنسب لـ:** السياق المختلط الذي يتضمن سجلات أدوات كبيرة إلى جانب تعليمات بشرية أو ملخصات المساعد.

---

## حساب وفورات المصدر الأساسي

يوثّق OmniRoute وفورات الضغط من مصدرين: معايير أداء المشاريع الأصلية ودمج
المحركات الخاص بـ OmniRoute.

| المصدر  | الرقم المستخدم هنا من README للمصدر الأساسي                                                                                              |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Caveman | عدد رموز مخرجات أقل بنسبة `~75%`، ومتوسط وفورات المخرجات في الاختبارات المعيارية `65%`، ونطاق `22-87%`، وأداة لضغط المدخلات بنسبة `~46%` |
| RTK     | وفورات بنسبة `60-90%` في مخرجات الأوامر؛ جلسة نموذجية من `~118,000 -> ~23,900` رمزًا، أي توفير بنسبة `79.7%` (`~80%`)                    |

بالنسبة إلى حمولات الأدوات/السياق المتداخلة، تجمع توليفة OmniRoute الافتراضية المحركات:

```txt
RTK -> Caveman
```

الوفورات المجمّعة تراكمية بالضرب، وليست بالجمع:

```txt
المجمّع = 1 - (1 - وفورات RTK) * (1 - وفورات مدخلات Caveman)
المتوسط = 1 - (1 - 0.80) * (1 - 0.46) = 89.2%
النطاق  = 1 - (1 - 0.60..0.90) * (1 - 0.46) = 78.4-94.6%
```

ينطبق رقم `78-95%` عندما يستطيع كلٌّ من RTK وCaveman تقليل حمولة المدخلات/السياق نفسها.
يُعد وضع مخرجات استجابة Caveman منفصلًا: عند تمكينه، استخدم وفورات مخرجات Caveman نفسها (متوسط
`65%`، ورقم رئيسي `~75%`، ونطاق `22-87%`). تعتمد وفورات الفوترة الإجمالية على مزيج المطالبات/المخرجات لديك.

### ما الذي تعنيه كلمة "مؤهل" فعليًا

النطاق الرئيسي 15-95% حقيقي، لكنه لا ينطبق إلا على المحتوى **المكرر أو المسهب** — مثل أسطر
الأخطاء المتكررة، أو سجل بناء يكرر التحذير نفسه بإفراط، أو مخرجات ضخمة من `grep`/قراءة ملف. ولا
يعني ذلك أن كل طلب يحقق هذا القدر من التوفير.

تم التحقق تجريبيًا (`tests/unit/compression/stacked-compression-tool-result-savings.test.ts`): أدى
تشغيل `stacked` ‏(RTK + Caveman) على كتلة `tool_result` ببنية Anthropic تحتوي على 300 سطر خطأ
متطابق إلى **وفورات في الرموز بنسبة 95.93% / وفورات في الأحرف بنسبة 96.26%** — ضمن النطاق المُعلن
تمامًا. لكن تشغيل المسار نفسه على مخرجات أدوات عادية وغير مكررة (قائمة مطابقة نظيفة من `grep`،
أو قراءة ملف قصيرة، أو نص محادثة اعتيادي) ينتج، على نحو صحيح، **وفورات تقترب من الصفر**، لأنه
لا يوجد شيء متكرر لإزالته، ولأن `validateCompression()` ‏(`validation.ts`) يرفض إرسال إعادة
صياغة قد تحذف أو تغيّر كتل التعليمات البرمجية أو عناوين URL أو العناوين أو الإصدارات أو معرّفات الثوابت المكتوبة بالكامل بأحرف كبيرة.

هذا سلوك متوقع وآمن، وليس خطأً برمجيًا: ستحقق جلسة برمجة تعتمد غالبًا على قراءة/البحث في ملفات نظيفة
وفورات إجمالية متواضعة حتى مع تمكين الضغط بالكامل، بينما ستحقق جلسة تواجه حلقة فاشلة
أو أداة lint كثيرة الرسائل النطاق الكامل 78-95% على ذلك التدفق. لا تستخدم انخفاض نسبة الوفورات
الإجمالية لجلسة واحدة دليلًا على سوء إعداد الضغط — تحقّق أولًا مما إذا كانت
مخرجات الأداة الأساسية مكررة فعلًا.

---

## عرض مرئي لوفورات الرموز

```
دون ضغط:             47K رمزًا مُرسلًا إلى LLM
باستخدام Lite:       40K رمزًا مُرسلًا          (توفير 15% — آمن، ومفعّل دائمًا)
باستخدام Standard:   33K رمزًا مُرسلًا          (توفير 30% — قواعد أسلوب Caveman)
باستخدام Aggressive: 24K رمزًا مُرسلًا          (توفير 50% — التقادم + التلخيص)
باستخدام Ultra:      12K رمزًا مُرسلًا          (توفير 75% — التقليم الاستدلالي)
باستخدام RTK:        19K-5K رمزًا مُرسلًا       (توفير 60-90% في مخرجات الأوامر/الأدوات)
باستخدام Stacked:    10K-2.5K رمزًا مُرسلًا     (نطاق RTK+Caveman المؤهل البالغ 78-95%)
```

---

## التهيئة

### لوحة التحكم

انتقل إلى `Dashboard → Context & Cache`:

- **Caveman** — تحديد الوضع، وحزم اللغات، والمعاينة، والإعدادات الافتراضية العامة
- **RTK** — معاينة مرشّح الأوامر، وإعدادات أمان RTK، ودليل المرشّحات
- **مجموعات الضغط** — مسارات معالجة مسمّاة للمحرّكات تُسنَد إلى مجموعات التوجيه
- **عتبة التشغيل التلقائي** — تشغيل الضغط تلقائيًا عندما يتجاوز عدد الرموز العتبة

### التجاوز لكل مجموعة

في `Dashboard → Context & Cache → Compression Combos`، أسنِد مجموعة ضغط إلى مجموعة توجيه:

```txt
المجموعة: "free-tier-fallback"
  مجموعة الضغط: "coding-agent-stack"
  مسار المعالجة: RTK -> Caveman
  الأهداف:
    1. if/kimi-k2.7-code
    2. if/qwen3.8-max-preview
```

يتيح لك ذلك استخدام الضغط المتسلسل مع موفّري الخدمات المجانية/البرمجية، مع الإبقاء على الوضع الخفيف للاشتراكات المدفوعة.

يُعد إسناد "التجاوز لكل مجموعة" عنصر تحكم مختلفًا عن تجاوز **وضع ضغط مجموعة التوجيه** (افتراضي/متوقف/خفيف/قياسي/قوي/فائق) — فهذا التجاوز لا يختار مسار معالجة مسمّى لمجموعة الضغط؛ بل يضبط فقط الحقل `compressionMode` الذي تستشيره `resolveCompressionPlan`. ويمكن ضبطه إما في بطاقة المجموعة (`Dashboard → Combos`) أو، منذ
#6760، لكل مجموعة توجيه ضمن قائمة "Assign to routing" في
`Dashboard → Context & Cache → Compression Combos`، بجوار مربع اختيار إسناد مسار المعالجة الموثّق أعلاه مباشرةً. تُحفَظ الإعدادات من كلتا الواجهتين عبر نقطة النهاية نفسها `PUT /api/combos/{id}`.

### التجاوز لكل طلب

أرسل ترويسة الطلب `x-omniroute-compression` لتجاوز خطة الضغط لطلب واحد. ولها أعلى أولوية — إذ تتغلب على تجاوز مجموعة التوجيه، والملف النشط، والتشغيل التلقائي، والإعداد الافتراضي للوحة. تُتجاهل القيم غير المعروفة (ولا يُرفض الطلب مطلقًا)، ويظل مفتاح التشغيل العام هو المتحكم في كل شيء: فعندما يكون الضغط متوقفًا عمومًا، لا يمكن للترويسة تشغيله. القيم:

| القيمة        | التأثير                                                                                   |
| ------------- | ----------------------------------------------------------------------------------------- |
| `off`         | لا يُطبَّق أي ضغط على هذا الطلب.                                                          |
| `default`     | الملف الافتراضي المستمد من اللوحة (يتجاهل الملف النشط). تظل المحرّكات الفاقدة متوقفة.     |
| `safe`        | مماثل لعدم تضمين الترويسة: إزالة التكرار ودمج المسافات البيضاء فقط.                       |
| `allow-lossy` | الاحتفاظ بخطة المشغّل لهذا الطلب، بما يشمل الملخصات، ومرشّحات الصلة، وإعادات صياغة النمط. |
| `engine:<id>` | محرّك واحد عند تمكينه، مثل `engine:rtk`. هذا هو الاشتراك لكل طلب لذلك المحرّك.            |
| `<combo>`     | مجموعة مسمّاة، تُطابَق أولًا حسب الاسم (من دون حساسية لحالة الأحرف)، ثم حسب المعرّف.      |

من دون `allow-lossy` أو `engine:<id>` أو مجموعة مسمّاة، لا تُطبَّق المحرّكات الفاقدة. ويظل الطلب يستفيد من إزالة تكرار الجلسة ودمج المسافات البيضاء عندما يكون الضغط قيد التشغيل.

تُعاد الخطة المطبّقة في ترويسة الاستجابة `X-OmniRoute-Compression: <mode>; source=<source>`، حيث تكون `<source>` واحدة من `request-header` أو `routing-override` أو `active-profile` أو `auto-trigger` أو `default` أو `off`.

### واجهة API

```bash
# الحصول على إعدادات الضغط
curl http://localhost:20128/api/settings/compression

# تحديث إعدادات الضغط
curl -X PUT http://localhost:20128/api/settings/compression \
  -H "Content-Type: application/json" \
  -d '{"defaultMode":"stacked","autoTriggerMode":"stacked","autoTriggerTokens":32000}'

# معاينة حمولة RTK/stacked محددة
curl -X POST http://localhost:20128/api/compression/preview \
  -H "Content-Type: application/json" \
  -d '{"mode":"rtk","messages":[{"role":"tool","content":"npm test output here"}]}'

# عرض قائمة حزم مرشّحات RTK
curl http://localhost:20128/api/context/rtk/filters

# اختبار RTK مباشرةً باستخدام بيانات وصفية اختيارية للأمر
curl -X POST http://localhost:20128/api/context/rtk/test \
  -H "Content-Type: application/json" \
  -d '{"command":"npm test","text":"FAIL tests/example.test.ts\nError: boom"}'
```

---

## ما الذي تتم حمايته

يحافظ محرك الضغط **دائمًا على:**

- ✅ كتل التعليمات البرمجية (المسيّجة والمضمّنة)
- ✅ عناوين URL ومسارات الملفات
- ✅ بُنى JSON والبيانات المنظّمة
- ✅ المعرّفات والرموز التقنية المحمية
- ✅ التعبيرات الرياضية
- ✅ تعريفات استدعاءات الأدوات/الدوال
- ✅ مطالبات النظام (في الوضع الخفيف)

تعمل استعادة المخرجات الأولية في RTK على تنقيح مفاتيح API الشائعة، ورموز الحامل، ورموز Slack، ومفاتيح وصول AWS،
وكلمات المرور، والرموز، والأسرار قبل حفظ أي شيء.

---

## إحصائيات الضغط

يتضمن كل طلب مضغوط إحصائيات في سجلات الخادم:

```json
{
  "originalTokens": 47200,
  "compressedTokens": 40120,
  "savingsPercent": 15.0,
  "techniquesUsed": ["collapseWhitespace", "dedupSystemPrompt"],
  "mode": "lite",
  "engine": "caveman",
  "compressionComboId": "coding-agent-stack",
  "durationMs": 0.8,
  "rtkRawOutputPointers": []
}
```

---

## خارطة طريق المراحل

| المرحلة    | الأوضاع                                                                                                                                                    | الحالة        |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| المرحلة 1  | متوقف، خفيف                                                                                                                                                | ✅ تم الإصدار |
| المرحلة 2  | قياسي، مكثّف، فائق                                                                                                                                         | ✅ تم الإصدار |
| المرحلة 3  | RTK، مكدّس، مجموعات الضغط                                                                                                                                  | ✅ تم الإصدار |
| المرحلة 4  | أنماط الإخراج، الوضع الفائق من فئة SLM، إطار التقييم                                                                                                       | ✅ تم الإصدار |
| المرحلة 4C | ميزانية سياق تكيّفية ("قرص ضبط") — محرك الحوسبة + API (`contextBudget` في `PUT /api/settings/compression`) + عناصر التحكم في الوضع/السياسة بلوحة المعلومات | ✅ تم الإصدار |

---

## شكر وتقدير

قواعد الضغط في الوضع القياسي مستوحاة من **[Caveman](https://github.com/JuliusBrussee/caveman)** الذي أنشأه **[JuliusBrussee](https://github.com/JuliusBrussee)** (⭐ 51K+) — المشروع واسع الانتشار المعروف بعبارة "لماذا نستخدم رموزًا كثيرة عندما تؤدي رموز قليلة الغرض". يفيد Caveman بتحقيق عدد أقل من رموز المخرجات بنسبة `~75%`، ومتوسط توفير في مخرجات الاختبارات المعيارية بنسبة `65%`، ونطاق توفير في المخرجات يبلغ `22-87%`، وأداة لضغط المدخلات بنسبة `~46%`.

وضع RTK مستوحى من **[RTK - Rust Token Killer](https://github.com/rtk-ai/rtk)** الذي أنشأته **[RTK AI](https://github.com/rtk-ai)** — وهو مشروع عالي الأداء لضغط مخرجات الأوامر الخاصة بالطرفية والبناء والاختبار وgit وتصفية مخرجات الأدوات. يفيد RTK بتحقيق توفير بنسبة `60-90%`، بينما تُظهر جلسة العينة في README توفيرًا قدره `~80%`.

---

## أنظمة الضغط المتقدمة

بالإضافة إلى الأوضاع السبعة القياسية، يتضمن OmniRoute العديد من أنظمة الضغط المتقدمة التي تعمل تلقائيًا بناءً على السياق.

### الضغط المدرك لذاكرة التخزين المؤقت

تدعم بعض المزودين (مثل Anthropic مع التخزين المؤقت للمطالبات) **التخزين المؤقت للمطالبات**، مما يسمح لهم بتخزين أجزاء من المطالبة لتقليل التكاليف وزمن الوصول. عند تمكين التخزين المؤقت، يمكن أن يؤدي الضغط الشديد في الواقع إلى **الإضرار** بالأداء لأنه يغير الرموز المخزنة مؤقتًا، مما يبطل ذاكرة التخزين المؤقت.

تحل وحدة `cachingAware.ts` هذه المشكلة عن طريق **اكتشاف سياق التخزين المؤقت** و **تعديل استراتيجية الضغط** وفقًا لذلك.

#### كيف يعمل

1. **اكتشاف سياق التخزين المؤقت** — يفحص نص الطلب بحثًا عن علامات `cache_control`
2. **تحديد مزودي التخزين المؤقت** — يتحقق مما إذا كان المزود المستهدف يدعم التخزين المؤقت
3. **تعديل الاستراتيجية** — يخفض `aggressive`/`ultra` إلى `standard` لمزودي التخزين المؤقت
4. **تخطي مطالبة النظام** — عادةً ما يتم تخزين مطالبات النظام مؤقتًا، لذا لا تضغطها
5. **استخدام تحويلات حتمية** — استخدم فقط التحويلات التي تنتج مخرجات متسقة

#### مثال على الكود

```ts
import {
  detectCachingContext,
  getCacheAwareStrategy,
} from "@omniroute/open-sse/services/compression/cachingAware";

const body = {
  model: "anthropic/claude-sonnet-4.5",
  messages: [{ role: "user", content: "Hello" }],
  cache_control: { type: "ephemeral" }, // ← Cache marker
};

const ctx = detectCachingContext(body, { provider: "anthropic" });
// → { hasCacheControl: true, provider: "anthropic", isCachingProvider: true }

const strategy = getCacheAwareStrategy("aggressive", ctx);
// → { strategy: "standard", skipSystemPrompt: true, deterministicOnly: true }
```

#### متى تستخدم

الضغط المدرك لذاكرة التخزين المؤقت **يعمل دائمًا** — لا حاجة للتكوين. يعمل فقط عندما:

- يحتوي الطلب على علامات `cache_control`
- يدعم المزود المستهدف التخزين المؤقت للمطالبات (Anthropic، OpenAI، إلخ.)

### التقادم التدريجي

تتراكم المحادثات الطويلة العديد من أدوار الرسائل، ولكن الأدوار الأقدم تصبح أقل أهمية. تقوم وحدة `progressiveAging.ts` بـ **تدهور الرسائل حسب مسافة الدور**:

- **الأدوار الحديثة (0-3)**: يتم الاحتفاظ بها حرفيًا (تفاصيل كاملة)
- **الأدوار المتوسطة (4-8)**: ضغط خفيف (مسافات بيضاء، تنظيف التنسيق)
- **الأدوار القديمة (9+)**: ضغط بدائي (إزالة الحشو، تلخيص)
- **الأدوار القديمة جدًا (20+)**: تلخيص مكثف أو إسقاط

#### مثال على الكود

```ts
import { applyAging } from "@omniroute/open-sse/services/compression/progressiveAging";

const messages = [
  { role: "system", content: "You are a helpful assistant" },
  { role: "user", content: "What is 2+2?" },
  { role: "assistant", content: "4" },
  // ... 50 more turns ...
];

const { messages: aged, saved } = applyAging(messages, {
  verbatim: 3, // First 3 turns: verbatim
  light: 8, // Turns 4-8: lite compression
  moderate: 20, // Turns 9-20: caveman compression
  // Turns 21+: heavy summarization
});

// saved = number of tokens saved
```

#### متى تستخدم

التقادم التدريجي **يعمل دائمًا** للأوضاع `aggressive` و `ultra`. وهو فعال بشكل خاص لـ:

- جلسات الترميز طويلة الأمد
- المحادثات متعددة الأيام
- سير العمل الوكيل مع العديد من استدعاءات الأدوات

### وضع إخراج الرجل الكهفي

تقوم وحدة `outputMode.ts` بحقن **تعليمات مطالبة النظام** لجعل النموذج نفسه ينتج مخرجات مضغوطة ومختصرة (نمط "الرجل الكهفي").

#### كيف يعمل

بدلاً من ضغط الإدخال، يضيف هذا الوضع مطالبة نظام مثل:

> "أجب بأقل عدد من الكلمات. تخطى المجاملات. استخدم جملًا قصيرة."

يعمل هذا بشكل جيد بشكل خاص لـ:

- توليد الكود (مخرجات أكثر اختصارًا = رموز أقل)
- الأسئلة والأجوبة السريعة (لا حاجة لشروحات مفصلة)
- المعالجة الدفعية (زيادة الإنتاجية)

#### متى تستخدم

وضع إخراج الرجل الكهفي **اختياري** — قم بتعيينه عبر تكوين التحرير والسرد:

```json
{
  "strategy": "auto",
  "config": {
    "auto": {
      "outputMode": "caveman"
    }
  }
}
```

### أنماط الإخراج (الكتالوج)

وضع إخراج الرجل الكهفي أعلاه هو **المسار القديم ذو النمط الواحد**. قامت المرحلة 4 بتعميمه إلى كتالوج من أنماط الإخراج القابلة للتركيب: `OUTPUT_STYLE_CATALOG` في `open-sse/services/compression/outputStyles/catalog.ts`. كل نمط هو تعليمات مطالبة نظام تجعل النموذج نفسه ينتج مخرجات أرخص؛ يمكن تمكين الأنماط معًا ويتم حقنها بترتيب الكتالوج.

| النمط                       | `id`          | ما يفعله                                                                                                                                                                                    | لغات التعليمات                                                    |
| :-------------------------- | :------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------------------------------------------------------------- |
| نص موجز                     | `terse-prose` | حذف الحشو/المقالات/التحوط؛ الحفاظ على الجوهر التقني دقيقًا. نفس النص مثل وضع إخراج الكهف القديم (مشار إليه، لم يتم إعادة كتابته).                                                           | en, pt-BR, es, de, fr, it, ru, zh, ja, id, vi                     |
| كود أقل                     | `less-code`   | سلم YAGNI: أصغر تغيير وظيفي، لا توجد تجريدات غير مطلوبة.                                                                                                                                    | en, pt-BR, es, de, fr, it, ru, zh, ja, id, vi                     |
| ذيل الحصان (مطور كبير كسول) | `ponytail`    | "أفضل كود هو الكود الذي لم يُكتب أبدًا": إعادة الاستخدام > إعادة الكتابة، السبب الجذري > العرض، أقصر فرق عمل.                                                                               | en, pt-BR, es, de, fr, it, ru, zh, ja, id, vi                     |
| لدي ADHD (العمل أولاً)      | `i-have-adhd` | العمل أولاً (الأمر/المسار/المقتطف قبل النثر)، خطوات مرقمة ومحددة، خطوة تالية ملموسة واحدة، لا مقدمة/ملخص/خاتمة. مقتبس من [ayghri/i-have-adhd](https://github.com/ayghri/i-have-adhd) (MIT). | en, pt-BR, es, de, fr, it, ru, zh, ja, id, vi                     |
| CJK موجز (文言)             | `terse-cjk`   | نمط صيني كلاسيكي موجز للغاية.                                                                                                                                                               | zh (محدد حسب المنطقة: يُعرض فقط عندما تكون اللغة المحددة هي `zh`) |

كل نمط يأتي بثلاثة مستويات شدة — `lite`، `full`، `ultra` — وكل مستوى
ينتهي بشرط الحدود المشتركة، الذي يحافظ على كتل الكود، مسارات الملفات، الأوامر،
سلاسل الأخطاء، عناوين URL والمعرفات كما هي.

#### كيف يعمل الحقن

`applyOutputStyles()` (`open-sse/services/compression/outputStyles/apply.ts`) يحل
الاختيار مقابل الكتالوج (يتم إسقاط المعرفات غير المعروفة والأنماط غير المتطابقة مع اللغة،
ولا يحدث خطأ أبدًا)، يربط التعليمات المختارة بترتيب الكتالوج،
يلحق شرط الحدود **مرة واحدة**، ويبدأ الكتلة بعلامة تكرار واحدة
(`[OmniRoute Output Styles]`)، لذا فإن إعادة التطبيق لا تفعل شيئًا. عندما تكون اللغة
المحددة (انظر اختيار اللغة أدناه) تحتوي على ترجمة، يتم حقن التعليمات المترجمة
بدلاً من الإنجليزية.

على نص يحتوي على `messages`، يتحقق تجاوز المحتوى (`shouldBypassCavemanOutputMode()` في
`open-sse/services/compression/outputMode.ts`) من آخر ثلاث رسائل ويتخطى
الأنماط للدورة بأكملها عندما تتطابق مع كلماتها الرئيسية المتعلقة بالأمان، والإجراءات غير القابلة للإلغاء،
والتوضيح، أو الحساسة للترتيب. يعمل التجاوز بغض النظر عن إعداد تبديل **تجاوز الوضوح التلقائي**
في لوحة التحكم (`cavemanOutputMode.autoClarity`).

عندما يسمح التجاوز بالدورة، `placeSystemInstruction()` (نفس الملف)، الذي
لا ينشئ أبدًا `messages[0]` جديدًا، يضع الكتلة في أول هذه الرسائل التي يجدها:

1. رسالة نظام رئيسية تحتوي على محتوى نصي: يتم إلحاق الكتلة بعد نصها.
2. حقل `system` على المستوى الأعلى: يتم إلحاق الكتلة بعد نص سلسلة، أو
   تضاف ككتلة نصية جديدة إلى مصفوفة كتل المحتوى.
3. أول رسالة نظام لاحقة تحتوي على محتوى نصي: يتم إلحاق الكتلة بعد نصها.
4. لا شيء مما سبق: تذهب الكتلة إلى رسالة نظام جديدة في نهاية `messages`.

على نص بدون `messages`، يتم إلحاق الكتلة بحقل `instructions` نصي،
أو تصبح `instructions` عندما يحتوي النص على `input` (سلسلة أو مصفوفة). يتم تخطي نص
لا يحتوي على `instructions` ولا `input` كـ `no_messages`.

#### كيفية التمكين

في لوحة التحكم: **السياق ← الإعدادات ← الضغط** — صف واحد لكل نمط مع
مفتاح تشغيل/إيقاف ومحدد مستوى. برمجيًا، يحتفظ تكوين الضغط
بالتحديد على النحو التالي:

```json
{
  "outputStyles": [
    { "id": "i-have-adhd", "level": "full" },
    { "id": "less-code", "level": "lite" }
  ]
}
```

التوافق مع الإصدارات السابقة: لا يزال إعداد `outputMode: "caveman"` القديم يعمل ويُعيّن إلى
`terse-prose`، متطابق بايتًا مع الحقن القديم في كل لغة قديمة.

اختيار اللغة: مع تشغيل `languageConfig.enabled`، يختار `autoDetect`
لغة أحدث رسالة للمستخدم (نفس الكاشف مثل محركات الإدخال)؛
إيقاف تشغيل `autoDetect` يثبت `defaultLanguage`. إيقاف ← الإنجليزية.

يتم تثبيت مصفوفة النمط × اللغة بواسطة
`tests/unit/compression/output-styles-i18n-matrix.test.ts`: لا يمكن شحن نمط جديد
بدون ترجمة برتغالية برازيلية على الأقل (أو استثناء صريح متعقب)، ولا يمكن
لنمط موجود أن يفقد لغة محلية بصمت. لإضافة نمط، انظر
[EXTENDING_COMPRESSION.md](./EXTENDING_COMPRESSION.md#adding-an-output-style).

### ضغط نتائج الأداة

توفر وحدة `toolResultCompressor.ts` **5 استراتيجيات ضغط متخصصة**
لنتائج الأدوات (استدعاءات الوظائف، مخرجات الوكيل، نتائج البحث، إلخ):

1. **ضغط نتائج البحث** — يزيل النتائج الزائدة، ويحتفظ بأفضل N
2. **ضغط قراءة الملفات** — يقتطع الملفات الكبيرة، ويحافظ على الرؤوس/الاستيرادات
3. **ضغط تنفيذ الكود** — يحتفظ فقط بـ stdout/stderr الأساسي
4. **ضغط استعلام قاعدة البيانات** — يحد من الصفوف، ويزيل البيانات الوصفية المطولة
5. **ضغط استجابة API** — يزيل الحقول الفارغة، ويكثف المصفوفات

#### متى تستخدم

ضغط نتائج الأدوات يكون **دائمًا قيد التشغيل** عند وجود استدعاءات للأدوات. لا يلزم أي تهيئة.

### خط أنابيب مكدس (Stacked Pipeline)

يعمل الوضع المكدس على تشغيل **محركات متعددة بالتسلسل** — عادةً RTK أولاً (توفير 60-90% في مخرجات الأداة)، ثم Caveman (توفير إضافي بنسبة 30% على النص المتبقي). يحقق هذا **توفيرًا إجماليًا بنسبة 78-95%**.

#### كيف يعمل

```
Input (1000 tokens) // المدخلات (1000 رمز)
  → RTK (command-aware filter) → 200 tokens // RTK (مرشح يراعي الأوامر) → 200 رمز
    → Caveman (filler removal) → 140 tokens // Caveman (إزالة الحشو) → 140 رمز
  → Output (140 tokens, 86% savings) // المخرجات (140 رمزًا، توفير 86%)
```

#### متى تستخدمه

استخدم الوضع المكدس من أجل:

- سير العمل الذي يعتمد بشكل كبير على الأدوات (البرمجة الوكيلة، البحث)
- المعالجة الدفعية الحساسة للتكلفة
- عندما تحتاج إلى أقصى قدر من توفير الرموز

قم بالتهيئة عبر التجميع:

```json
{
  "strategy": "auto",
  "config": {
    "auto": {
      "modePack": "stacked"
    }
  }
}
```

---

## تجاوزات الضغط لكل توليفة

يمكنك تجاوز وضع الضغط العام **لكل توليفة** لضبط السلوك بدقة
لحالات الاستخدام المختلفة:

```json
{
  "id": "coding-combo",
  "strategy": "priority",
  "config": {
    "auto": {
      "weights": { "taskFit": 0.5 },
      "modePack": "quality-first"
    }
  },
  "compressionOverride": {
    "mode": "aggressive",
    "stackedPipelines": ["rtk", "caveman"],
    "preserveToolDefinitions": true
  }
}
```

يفيد ذلك في:

- **توليفات البرمجة**: استخدم وضع `aggressive` للجلسات الطويلة
- **توليفات الأسئلة والأجوبة السريعة**: استخدم وضع `lite` للاستجابات السريعة
- **التوليفات كثيفة استخدام الأدوات**: استخدم وضع `stacked` لتحقيق أقصى قدر من التوفير
- **توليفات بيئة الإنتاج**: استخدم وضع `cache-aware` لموفّري التخزين المؤقت

---

## راجع أيضًا

- [إعدادات البيئة](../reference/ENVIRONMENT.md) — متغيرات بيئة الضغط
- [دليل البنية](../architecture/ARCHITECTURE.md) — التفاصيل الداخلية لمسار الضغط
- [دليل المستخدم](../guides/USER_GUIDE.md) — بدء استخدام الضغط
- [ضغط RTK](./RTK_COMPRESSION.md) — عوامل تصفية RTK، ونموذج الثقة، وبوابة التحقق، واسترداد المخرجات الأولية
- [محركات الضغط](./COMPRESSION_ENGINES.md) — Caveman وRTK والوضع المكدّس وواجهات API وMCP ولوحة المعلومات
- [تنسيق قواعد الضغط](./COMPRESSION_RULES_FORMAT.md) — تنسيق حزمة القواعد بصيغة JSON
- [حزم لغات الضغط](./COMPRESSION_LANGUAGE_PACKS.md) — قواعد Caveman الخاصة بكل لغة
