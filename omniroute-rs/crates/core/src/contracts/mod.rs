//! Locked plugin contracts. Each sub-module is a frozen wire shape.
//!
//! Versions here are stable contracts, not APIs. Bumping a version is
//! a breaking change for downstream consumers and must be paired with
//! a contract-migration note in the relevant ADR.

pub mod opencode_v1;
